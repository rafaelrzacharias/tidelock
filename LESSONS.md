# tidelock — lessons & gotchas (one line each; read before build work)

- **IDE = VS Code + CMake Tools (presets) + clangd + CodeLLDB**; no Visual Studio solutions. `.vscode/` is committed; put machine-local tweaks in `.vscode/*.local.json` (ignored). Toolchain on this PC: LLVM 22.1.7 (`C:\Program Files\LLVM\bin`), CMake 4.3, Ninja 1.13, Python 3.13.
- clangd reads `.cache/compile_commands.json`, which CMake Tools copies from the active preset — configure once before expecting diagnostics.
- Binaries land in `out/<preset>/bin/`; `out/` and `.cache/` are ignored.
- **clang-cl is a `cl` driver, not a `clang` driver**: `-Wall` there means MSVC's `/Wall`, which clang maps to `-Weverything` (`-Wc++98-compat` on every C++20 line). Use `/W4 /WX`, `/EHs-c-`, `/GR-`, `/Zc:threadSafeInit-`, `/clang:-nostdinc++`; the other `-W`/`-f` flags are the same in both modes (`docs/BUILD.md` §9 R-6).
- `CMAKE_BUILD_TYPE` is forced empty — tiers own the flags. Leave it and CMake silently appends its own `/Ob0 /Od /RTC1 -MDd` set, which is a `build_id` input nobody chose (`docs/BUILD.md` §9 R-5).
- CMake presets are only read from `CMakePresets.json` at the repo root; ours is two lines that `include` `cmake/presets.json`.
- In CMake regexes write parentheses as `[(]` / `[)]`: quoted-argument escaping and the regex engine disagree about backslashes, and a mis-escaped pattern silently matches nothing (it cost a build cycle in the test-list generator).
- A static lib needs at least one TU: every empty module folder carries one placeholder `.cpp` until its lane lands. Module sources are globbed with `CONFIGURE_DEPENDS`, so a lane adds files without editing another lane's CMake.
- `tl_foundation_det` vs `tl_foundation` is split by an explicit NON-det stem list — anything new defaults to the audited half, so a mistake fails the symbol audit loudly instead of escaping it.
- An audit is worth what its **negative test** is worth. The W0 gates passed every obvious plant and let five adversarial ones through (`f32` instead of `float`, a basename collision across exempt lists, an anonymous-namespace global, an upward `#include`, an upward symbol reference). Plant the clever violation, not the obvious one.
- Regexes cannot see mutable globals — anonymous namespaces, `inline static`, dynamic initialisers all evade them. `llvm-objdump -h` and a zero-`.data`/`.bss` rule can, and cost nothing.
- `build_id` over a *flag string* is a lie: compile definitions, `-std`, `CXXFLAGS` from the environment, `cmake/` edits and git-ignored sources all bypass it. Hash the resolved compile commands (`compile_commands.json`) plus the content of the files they name.
- `[[nodiscard]]` cannot attach to a typedef. An error code that must not be discarded has to be an `enum`, not `using ErrCode = u16`.
- A doc sentence that names no tool ("the fingerprint tool asserts it") is a phantom gate; grep for the tool before believing it.
- `build_id` had to answer one question, and it was answering two: "are we the same program?" (must be target-independent, or PC + Deck + Pi peers can never hand-shake) and "what exactly built this binary?" (compiler, triple, flags). Two questions, two values: `build_id` is compared, `build_env` is reported (`docs/BUILD.md` §9 R-8).
- clang spells it `-std=c++20`, clang-cl `-std:c++20`. Normalise driver spellings before they reach a hash, or two targets disagree on a value that is supposed to be identical.
- The audit selftest found four bugs in its first run - two in the fixtures, one in the new `build_id`, one real gate hole (a header's contract block was counting as the first function's contract comment). Write the negative test the same day as the gate.
- Fixed point is not enough on its own: `char` is signed on x86-64 and **unsigned on aarch64**, and `long` is 32-bit on Windows and 64-bit on Linux. A PC peer and a Pi peer diverge on either with no UB anywhere. The determinism moat needs the fixed-width vocabulary *enforced*, not just intended (`tools/audit/includes.py` bans them in sim TUs).
- A `static` function is internal linkage, not state. The `^static [^c]` grep in `CPP-SUBSET.md` §1 rejected every one of them — the gate would have blocked the first real `.cpp` written. Mutable-global detection belongs at the link stage (`.data`/`.bss`/TLS), not in a regex.
- `.data.rel.ro` is const data holding relocations, and PIE (the Linux/Pi default) is where const function-pointer tables land. A `.data` prefix match rejects it — passing on Windows and failing every lane in CI.
- `'` in C++ is a digit separator as often as a character literal (`1'000u`). A naive literal-stripper treats the rest of the file as a string and every token ban downstream goes silent.
- A gate that is only exercised by fixtures its own author wrote proves nothing. R-8 ("one `build_id` for every target") is now checked by building netcode-win and netcode-linux from one checkout in CI and diffing `build_id.txt`.
- Removing an input from a hash invalidates every argument that leaned on it. Taking the compiler out of `build_id` silently voided R-7's "an off-pin peer cannot join" — in the same document, in the same commit.
- Fixed-width types are not enough either: **bit-field layout** differs between windows-msvc and linux/aarch64 (measured: `{u8 a:4; u16 c:8;}` is 4 B vs 2 B), and `usize` as `size_t` is a different *type* per target even at the same width, so a `usize`-keyed template selects different code. Both are banned in sim TUs; `usize` is now `u64`.
- `const char*` is legal in sim code for message literals — which re-opens the `char` hole one level down: `(u64)s[i]` over a byte ≥ 0x80 sign-extends where `char` is signed. Sim **string literals** must be ASCII; comments are free (they never become char data).
- Backslashes do not survive this shell's heredocs: `` in a regex arrived as a literal backspace byte (``), so four new bans compiled fine and matched nothing. Print `pattern` after generating a regex programmatically.
- A patch script that asserts before writing loses every earlier edit in the same run. Three bans "landed" and vanished; only the selftest noticed.
- **Check CI before writing "verified" in a commit message.** Three pushes went out claiming verification while four jobs were red on main - including the job built to prove R-8. A green local run is evidence about the local machine, nothing more.
- A compile line is not whitespace-separated. CMake >= 4.4 quotes `"C:/Program Files/LLVM/bin/clang-cl.exe"`; `cmd.split()` made `build_id` depend on whether the compiler's install path contained a space. Use `shlex`.
- Dropping a flag but keeping its separate argument is worse than keeping both: `-isystem <dir>` and `-imsvc<dir>` became different token sets for the same directory.
- An exemption carved for one rule silently applies to every rule sharing the flag. `tl_types.h` was exempt from the float ban - and therefore from the bit-field, platform-macro and section bans too, in the one header every sim TU includes.
- A gate that compares two empty lists passes everything. `clang -E` line markers escape backslashes (`# 1 "C:\dir\f.cpp"`), so normalising `\` to `/` produced `c://dir//f.cpp`, matched nothing, and the cross-target gate reported 0 divergences on planted violations. It now fails loudly if no line of our own source reached the comparison.
- `-nostdinc` drops clang's resource dir as well as the platform's headers; `-nostdlibinc` keeps the freestanding `<stdint.h>`/`<stddef.h>`/`<limits.h>` and drops only the system ones. That difference is what lets the cross-target gate run with no sysroot.
- Two of four "the gate missed it" findings were wrong fixtures, not wrong gates: `#pragma pack(1)` alone packs identically everywhere, and `__GNUC__` is already undefined for the windows-msvc triple under the clang driver. Verify the fixture before believing the failure.
- **A filter must never decide what gets compared.** Twice now the same bug: a path filter that matched nothing compared two empty lists, then a record comparison keyed by a name parsed out of a dump dropped anonymous-namespace, unnamed-typedef, same-named-local and `__`-prefixed records. Both were silent passes. Compare positionally with a count check; use names only in messages.
- Filter sets must be **measured, not guessed**. The system records to skip now come from dumping a TU that includes only the sanctioned headers - a prefix match on `__`/`max_align_t` was user-controllable, so a struct named `__Cell` vanished.
- Normalising a difference away is as dangerous as failing to see it: stripping integer suffixes made `1LL` identical to `1` and hid a 64-bit-vs-32-bit shift. **Map** spellings to a canonical form that preserves width; never delete.
- `int64_t` is `long long` on windows-msvc and `long` on linux/aarch64 - the same type, two spellings. Every `fx<i64,FRAC>` in a layout dump reads as divergent until they are canonicalised.
- "0 false positives on this tree" is worth nothing when the tree is 13 placeholder files. Test a gate against the code that is about to be written, not the code that exists.
- A `static_assert` message is a string literal to the non-ASCII gate: `"docs/X.md §3.1"` fails a sim TU. Spell `section 3.1` in messages; comments are free.
- `using pos_t = fx<i32,18>` and `using invmass_t = fx<i32,18>` are ONE type: every "per row" trait, overload or op-table line collides with its format twin (the first fx_palette.h had six redefinitions). Key tables by format, or tag the rows (RR-5).
- Let the compiler check every documented constant: `static_assert(G_SUBSTEP.v == 21432)` failed - the doc's arithmetic was wrong, not the code. A raw value in a doc is a claim until a `static_assert` has seen it.
- A 2^30-entry reference table cannot be committed; an exhaustive sweep lives in `tools/` (doubles allowed), its worst cases are re-verified at 60 digits, and the MEASURED bound is a constant the unit tests assert against. "<= 2 ulp" in a spec written before the kernel existed was a guess (it measured 9.06).
- The trap stub of `tl_fatal` gives no message: a `TL_ASSERT` in a test shows as exit `0xC000001D` with nothing printed. Find it with `tl_tests --filter <name>` per test, or a scratch `main` that includes the `.test.cpp` and defines a printing `tl_assert_failed`.
- A seeded property test over full-range inputs will leave the helper's contract (`mul<angle_t>(omega_t, H)` at 2048 turn/s is 4 turns) and trap in dev. Generate inside the contract and COUNT what was tested, or the test is vacuous where it passes and a trap where it does not.
- The `(s + 1.5*2^23) - 1.5*2^23` rounding identity is exact only for `|s| <= 2^22`: above that the sum crosses 2^24, where a float's spacing is 2, and every odd integer comes back even. Use `2^23` per sign (`2^52` for doubles). The round-trip test tolerated 1 ulp - the exact size of the bug - so it passed; a tolerance must be derived from the claim, not from what makes the test green.
- **UBSan recovers by default.** It prints the report and the process exits 0, so a sanitizer CI lane without `-fno-sanitize-recover=all` is a log, not a gate: a signed overflow was reported in a green run of the W1 fx lane. Read the lane's log once, not just its badge.
- A float needs no `float` token: `decltype(1.0) x`, `auto y = 0x1p3`, `k * 1e3`, `_Float16`; a `long` needs no `long` token: `decltype(1L) x`. Token bans need literal bans beside them, and a local never reaches the layout gate.
- A pinned trace hash proves exactly the helpers the trace calls on the inputs it draws, nothing more. List what it skips in the test, or add a second pin; "1M values" is a size, not a coverage.
- A seeded property test's in-range count must be asserted PER generator, never as a sum: sixteen rows summed to "> 8N" lets one vacuous row hide behind fifteen full ones.
- The generated test list registers every `TL_TEST(` in the tree, `#if` or not: a tier-conditional test puts the `#if` INSIDE the body (with a trivial check on the other branch), or the link fails with an undefined `test_*`.
- A `TL_ASSERT` that fires inside a test is a silent `0xC000001D` under the trap stub; the fastest way to find it is a scratch `main` that `#include`s the test body with a printing `tl_assert_failed` (it found `fx.h:272` and a bad divisor in trace B in under a minute each).
- Patching a source file through a Python non-raw triple-quoted string eats `\` + newline: a multi-line macro's continuations vanish and the macro becomes one 400-column line that still compiles. Use raw strings or the editor for anything with line continuations.
- A zero-on-push that starts at the aligned block start misses the **alignment gap** [old used, start): the gap enters [base, used), which is exactly what the state hash covers, so a reused-then-realigned registered arena hashes stale bytes with no UB anywhere. Zero from old `used`, and test the gap, not just the block.
- A vendored header can fail the cross-target layout gate for a branch that never executes: rapidhash.h's `#ifdef _MSC_VER` pulls in `<intrin.h>` on the win triple regardless of `__SIZEOF_INT128__` (clang predefines `_MSC_VER` for MSVC-triple compat, and its own resource-dir `intrin.h` then declares ~90 SIMD-intrinsic records linux/pi never see) even though the `_MSC_VER` multiply path is dead code on every target we build for. Stub the header (`intrin.h`, next to the existing `string.h` stub), never `-U` the macro: the first fix passed `-U_MSC_VER` in `BASE_FLAGS` and the gate then reported "0 divergences" for a sim TU that was literally two programs (`#ifdef _MSC_VER` around two different structs), because `_MSC_VER` is the ONE platform macro clang predefines for a triple we actually ship.
- **Undefining a macro to silence a gate deletes the gate, not the noise** - and the negative fixture that would have caught it did not exist because `__GNUC__`'s fixture "already covered macro branches": `__GNUC__` is undefined for the win triple, so that case never exercised the win-vs-linux leg at all. One fixture per macro that is predefined for a shipped triple.
- Vendoring a library that lives outside `src/` still needs two separate gate entries, not one: `tools/audit/includes.py`'s `SYS_ALLOW_DIRS` (which directory may spell the system include at all) and its `BACKEND_HEADERS` (which directory is its wrap module) check independently, and `tools/audit/targets.py` needs the vendor dir on its own `-I` list since it preprocesses standalone with no CMake target to inherit include dirs from.
- A golden computed by running the code it guards is a screenshot, not a known answer. Both hash families' vectors were "computed from the pinned implementation"; they turned out correct, but nothing in the test could have said so. `tools/rapidhash_ref.py` re-derives them from the algorithm in another language - the cheap independence that catches a wrong config branch, seed, secret or unroll boundary.
- A `[0, 1)` fraction does not make a `[lo, hi)` range: `lo + mul<R>(q, hi - lo)` rounds RNE, so every span under 2^29 raw units rounds its top draw up to exactly `hi`. And `hi - lo` is the row's *wrapping* subtract - `rng_range<pos_t>(r, -WORLD_HALF, WORLD_HALF)` wrapped the span to `INT32_MIN` and returned positions kilometres outside the world with no assert, while the test asserted `<= hi` and passed.
- A gate exemption keyed on a bare filename stem is not scoped at all: `log`, `prof`, `probe` and `crash` are ordinary words, and `symbols.py` matching the archive member's stem alone exempted a `log.o` in ANY `--data-only` lib - every non-audited lib in `src/`. The twin rule in `includes.py` was scoped to `src/foundation/` and looked identical in the diff. An exemption is a stem INSIDE a named scope; write the scope down or the gate has a hole the size of the vocabulary.
- The one header that crosses a firewall is the one an exemption must never touch: `tl_assert.h` is on the tooling stem list AND is the single tooling header a sim TU may include, so a stem-keyed io/state grant handed both to every det TU in the tree. `#include <stdio.h>` + a mutable global appended to it produced 0 violations.
- `#if UNDEFINED_MACRO <= 0` is true, silently: `TL_LOG_MIN` was specified per tier in `TOOLING.md` and defined in no build file, so every tier compiled in `TL_LOG_TRACE` and the compile-out test's `#else` arms were dead code in every tier it was ever built for. A tier-conditional constant that a header reads must be `#ifndef`-guarded with a derivation, not assumed present.
- A test whose `#if` mirrors the header's `#if` cannot fail on the branch the current tier does not take. To test a compiled-out call site, pin the gating macro in the test TU itself (before the include) so the barred branch is exercised in every tier.
- A `slow` tag does not keep a destructive test out of a run: the runner selects every test when no `--tag` is given, so a `TL_FATAL` trigger test killed the whole suite with exit 2 and reported nothing. Gate a process-ending test on something the caller must set (an env var), and tag it as well.
- `\n` written to stderr comes back as `\r\n` when a child's stderr is redirected to a file on Windows (text-mode stream). A marker contract spelled with `\n` in a doc must be matched as "line ending", not as those two bytes.
- Patching a source file through this shell's heredoc eats one backslash: `\n` in the Python source arrived as a real newline and broke three string literals. Build escapes with `chr(92)`, or edit the file directly.
- `CreateProcessA`'s `lpCurrentDirectory` must be an absolute path; a relative one is unreliable and, worse, a `WaitForMultipleObjects` array holding a handle from a failed spawn doesn't error out - it returns `WAIT_FAILED` and a naive retry-the-same-wait loop spins forever, which looks exactly like a hung test suite from the outside (found building the W1 runner lane's `--isolate` pool). Always resolve the private-cwd path to absolute before the spawn call, and treat `WAIT_FAILED` as "every active handle is presumed dead," not as "wait again."
- `taskkill /IM <name>.exe /T` kills every process with that image name on the machine, not just the one you started - on a two-PC-worktree setup with several lane sessions running concurrently, this silently kills a sibling worktree's in-progress `tl_tests.exe` run. Kill by exact PID (from your own spawn or `wmic process where "CommandLine like '%your-worktree-path%'"`), never by bare image name, when other sessions might be running the same binary.
- An exhaustive property test tagged `slow` (2^30 iterations) left running under `--isolate` with no `--tag !slow` looks identical to a deadlock from the outside (one lone child process, no output, past any reasonable timeout) - it is just the straggler once every fast test has finished. Check what the lone survivor's `--run-one` index actually is (`tl_tests --list`) before concluding the pool hung; the PR lane's own invocation (`docs/TESTING.md` §9.3) already excludes `slow`.
- **A counter nobody reads is not a gate.** `TestCtx::checks` was incremented by every assertion macro in the runner and read by nothing, so a test whose checks all sat inside an untaken `#if` - the tier-conditional shape this file mandates - reported PASS having executed nothing. Six of them were live. Whenever you add a counter to prove work happened, add the line that FAILS when it is zero, in the same commit.
- **A failed spawn is not evidence.** A `CreateProcess`/`fork` failure that sets an "it terminated abnormally" flag makes every fatal-expected test pass: a broken exe path turns the whole fatal suite green while running none of it. Check "did it start" before interpreting "how did it end", always, and never let a not-run path share a code with an expected-outcome path.
- **A template with no call site has never been compiled.** `NEAR_FX`/`SPAN_EQ`/`MEM_EQ` shipped as "completed" with zero instantiations anywhere in the tree, so no compiler had type-checked them; the first one written found a signed-overflow UB in the ulp difference. A generic helper is not code until a test instantiates it.
- **A selection that matches nothing must be an error, not a clean run.** Third occurrence of the empty-list silent pass (after the cross-target gate and the record comparison): `tl_tests --tag <typo>` printed "0 run, 0 failed" and exited 0, and the PR lane's invocation is `--isolate --tag !slow`. Any filter, glob or tag that can select zero items needs a zero check beside it.
- **`atoll` answers 0 for `abc`, `` and `12x`.** `tl_driver --seed abc` was a silent seed 0 and `--ticks abc` a silent zero-tick run, in a binary whose contract is byte-identical output for the same seed. A CLI that takes numbers needs a strict parser that rejects, not a C library call that guesses.
- `docs/ROADMAP.md`'s dependency graph lists a lane's headline deps ("platform: skeleton"), not every type its implementation spec actually needs mid-build: `PLATFORM.md` §9.5 allocates impl state from mem's `VMemArena` in its very first init step, which the graph doesn't surface as a platform dependency at all. Read the target doc's implementation spec for real blocking types before trusting the wave graph's dependency column.
- `tools/audit/includes.py`'s contract-comment gate matches a struct's `RetType (*name)(args)` function-pointer field only if the FIRST paren pair it walks ends in `{`/`;`/etc: for a bare-identifier return type (`void`, `u32`, `ErrCode`...) that first pair is the pointer-declarator's own `(*name)`, whose close is immediately followed by the real `(args)` - starting with `(`, which the accepted-suffix list excludes - so it always reads as "not a function" and needs no comment. A macro that expands a helper's `{ return ...; }` onto its own physical line does NOT get this exemption - each such line is its own declaration site and needs a real comment above it, same as any hand-written function.
- `arena_push` aligns a push's START (`align_up(used, align)`), never its SIZE - `end = start + bytes` with no rounding (`MEMORY.md` §8.2, measured against the real `vmem_arena.cpp`, not the doc's pseudocode). A caller whose own contract promises an aligned GROWTH (`PLATFORM.md` §9.6 `read_all_contract`: "`used` grows by exactly `align16(len+1)`") has to round the SIZE it asks for itself; passing `align` alone silently leaves `used` unaligned whenever the payload length isn't already a multiple of it.
- A "no stray tmp file" promise on an atomic-write crash-safety test is a promise about the FINAL, uninstrumented call - not about every killed one. `write_atomic`'s temp file legitimately survives a kill at "after write" or "after fsync, before rename" (the process died before the rename that would remove it); asserting tmp-cleanliness inside the per-kill-point loop fails a design that is working exactly as specified. Check only what the doc actually promises at each point: the TARGET file's integrity during the loop, tmp-cleanliness only after the real call.
- A trigger test gated on an env var (`getenv(...) == nullptr) return;`) reports FAIL, not a silent no-op, when run bare without `--filter <name>` + the var set - the runner's zero-checks-is-a-failure rule (`TESTING.md` §1) applies to it like any other test. This is by design (matches `tl_assert_forced_fatal_trigger`'s own precedent): the trigger is misuse outside its one sanctioned call path, and the PR lane's `--isolate --tag !slow` never reaches it. Confirmed, not "fixed", when a slow-tagged trigger test fails under a manual `--tag slow` run with no env var set.
- **`windows.h`'s raw `min`/`max` macros mangle any declaration of a same-named function that comes after them.** `fx.h` declares free functions `min`/`max`; `tests/foundation/vmem_test_api.h` (`#include <windows.h>` with no `NOMINMAX`) had never been included from the same TU as an fx-family header until the W1 containers lane's `sort.test.cpp` (via `rng.h`) did - "too many arguments to function-like macro invocation" is what a mangled `constexpr fx<Rep,F> min(...)` looks like from clang, not an obvious min/max collision message. `NOMINMAX` before `<windows.h>`, always - now a rule for the whole tree rather than advice, enforced by `tools/audit/includes.py` gate 7 (`PLATFORM.md` §9.1), which is the one gate that walks `tests/` as well as `src/`.
- **`vmem_arena_init` rounding the reserve to the page size, not `COMMIT_GRANULE`, is not just a mem-lane test gap.** A `SlotMap` domain whose per-column bytes (`sizeof(element) * (IDX_MASK+1)`) land under 64 KB - true of most test geometries and some real small resource-handle pools - TL_FATALs "arena over reserve" on its very first `arena_push`, because the first commit always rounds its *want* up to a full `COMMIT_GRANULE` regardless of how small the reserve was. The fix landed on main the same day (`vmem_arena_init` rounds every reserve up to `COMMIT_GRANULE`, `docs/MEMORY.md` §8.2), and the caller-side floors written against the old behaviour are now dead code - delete them when you merge, do not leave them "harmless".
- **"Two instances, same op sequence" is the weakest determinism test that still looks like one.** Five of them shipped in the W1 containers lane and not one could see that `Array` and `Map` were leaving removed elements' bytes inside the hashed extent - both instances got the same garbage. The property worth testing is DIVERGENT histories that converge: build the same logical state two different ways and compare bytes. Where that is false by design (a ring's state is its two free-running counters), test the negation and say so, so nobody "fixes" it later.
- **A vmem-backed `Array`'s hashed extent is its whole committed capacity, not `[0, count)`** - the arena's `used` tracks `cap`. So `pop`, `swap_remove` and `clear` are determinism operations, not bookkeeping: whatever they leave above `count` is hashed. `ARENA_ZERO_ON_PUSH` does NOT clean it up on refill (it only re-zeroes bytes an `arena_push` actually walks over, and an in-place refill pushes nothing) - a sentence in `CONTAINERS.md` §8.1 claimed it did, and that claim is why the bug shipped.
- **Never let a container assume `arena_push` hands back zeros.** Fresh pages are zero; anything below the arena's `high_water` is zero only under `ARENA_ZERO_ON_PUSH`. `Map`'s `state` array assumed it, and a `Map` built from a reused arena reads every slot as FULL - so `map_probe`'s "walk to the first empty slot" never terminates. A hang, not a wrong answer.
- **A "reference implementation" oracle has to consume the output under test.** The 1M radix test insertion-sorted a *second* random array and asserted that array was sorted; it never read the sort's output, so every radix bug passed. Sort a copy of the SAME pairs and compare element-for-element - and check the value column is a permutation of the input indices, which is the cheapest way to catch "lost or duplicated an element".
- **A single-shot parallel test cannot see a job-to-job lifecycle bug.** Reintroducing the W1 jobs lane's R-4 defect (the barrier counting completed CHUNKS, so the worker that zeroes the count is still inside its claim loop and does one more `fetch_add` on a counter the next job has already reset) was caught by the contention soak and by **nothing else** - the worker-invariance sweep, the levels test, the scratch test and both shuffle tests all passed, at every worker count, because each runs one job per pool. The bug lives in the window between one job's last retire and the next job's publish. Any pool, queue or arena with a reuse cycle needs a test that runs the cycle thousands of times back to back, and that test's checker has to be in the fast lane - slow-tag it and the defect ships.
- **A balanced scratch scope leaves `used` at 0, so `used > 0` is not evidence that work happened.** A jobs test asserted `arena_mark() > 0` after 512 chunk fns had each pushed 4 KB and popped it; the arenas had done real work and the assertion was simply wrong. `high_water` is the field that remembers, and it is what a "these arenas were actually exercised" check must read.
- **A checker that spawns its own timed child must give it a deadline UNDER the lane's per-child budget** - the lane kills the checker at its budget and a deadlocked grandchild with a longer deadline is ORPHANED past that kill (measured: two orphaned soak triggers at 180 s under a 120 s lane budget squatted on tl_tests.exe and failed the next build with "permission denied"). The grandchild's timeout is deadlock detection, not headroom; size it from the child's real runtime, not from generosity.
- **An epoch guard (`if (e == seen) skip`) turns a counter wrap into a permanent stall wherever one side can advance without the other observing** - the jobs inline path advanced a u32 epoch without waking workers, so exactly 2^32 inline jobs between two pooled ones re-equalled a worker's `seen` and parked it on a real token. A compare-for-equality freshness token must be too wide to wrap (u64), or every advance must be observed by every comparer.
- **A double shadow is the first thing to run on a fixed-point failure, not the last.** Three of the W2 gate0 lane's "row failures" were solver design (an owner-only PBF pass launching a landing block, corner-order friction drifting a stack, a wall edge on an interior face) and the double world did every one of them identically; the one real row effect (per-constraint `lambda_t` narrowing creeping a resting box 12 quanta/tick) was the one where the double sat still. Attribution before ladder-climbing, every time.
- **Substepping amplifies an unconverged position correction by 1/h.** A one-pass PBF over-corrects a landing by ~3x; at 60 Hz that is a slow ripple, at 480 Hz the same 6 mm becomes a 2.9 m/s launch because v = dx/h. A constraint that is not converged per substep needs compliance scaling with h^2 or it needs iterations - more substeps make it worse, not better.
- **An analytic box SDF is ambiguous at every corner, and an aligned stack is nothing but corners.** `max(|lx| - hw, |ly| - hh)` at a corner-on-corner returns the x-axis normal on a tie; the whole stack got horizontal pushes and toppled. Break the tie with the querying body's centre, and never let a static wall's EDGE lie on another wall's interior face (a particle in the corner then reads the edge and is pushed through the floor).
- **A neighbour list that carries a support margin needs the capacity of the margin, not of the kernel.** Lists at radius h + travel hold ~50 at rest; capped at ALLOY's 64 they overflowed under 30% compression, the dropped neighbours under-estimated rho, and the liquid collapsed into itself - a cap on a list is a silent change to the physics.
- **"Any saturation is a FAIL" needs every narrowing to COUNT, not assert.** The first G-02b run died on a `TL_ASSERT` in a helper (a plank spun a quarter turn per substep); a bench that aborts on the very event it is meant to grade reports nothing. Clamp, count, print the count on the verdict line.
- **A bench that stops early must stop its statistics too.** `tl_gate0` kept `cost[ticks]` sized to the requested tick count, so a G-05 run that escaped at tick 139 of 500 took its p50/p95/p99 over a zero tail (every percentile 0), suppressed its own verdict line, and the README's cost numbers had to be hand-derived from a CSV column. Percentiles over the ticks that RAN, and the cost detail printed on the failure path too - a FAIL's cost is evidence, its verdict is not. Same class: a double shadow with no escape stop reports the distance a runaway particle travelled as "max |fx - double|"; read the ticks before the ejection.
- **Measure the kernel's ops before blaming the arithmetic.** RR-13's "fixed-point premium ~5-10x" was an estimate; a 40-line micro-benchmark of the pair body as spelled gave 176 ns fixed vs 5.5 ns double (32x), of which 125 ns was two bit-serial `isqrt64` calls and 51 ns three i64 divides - the kernel called `length()` and then `normalize()` (a second root) and divided by h separately. The palette rows contributed 4 ns. A cost verdict on a number system needs the op count and the per-op cost on the table, or it grades the implementation.
- A threshold and its measurement protocol must name the same core count. G-05's "4 ms at 20k" was `ALLOY.md` §11.2's ~8-core budget graded against a single-thread run — unreachable by ANY scalar arithmetic including double, which silently voided the pre-committed float-fallback clause it was supposed to arm (`GATE0-BENCH.md` §7 R-3).
- Derive a row's range from its structural cap, not its nominal domain. `omega_t` carried ±2,048 turn/s while `pθ = θ − ω·h` caps |ω| at `inv_h/2` = 240 by construction — ~90 % of the range was unreachable headroom that cost 4× resolution until Gate 0 measured it (`FX-PALETTE.md` §9 R-8).
- A scenario criterion that says "nothing fails" grades whatever is weakest, not what the scenario exists to test. G-02b existed to test the boulder's 4096:1 contact and its letter graded the feather's ejection (`GATE0-BENCH.md` §7 R-5). Name the body the criterion is about.
- A CSV named only by (scenario, substeps, ladder) lets a later probe run silently overwrite an earlier verdict's evidence: the `--perturb` probes re-wrote `G01_s8_l1.csv` over the 10k-tick verdict's trace — the exact class the W2 review's D3(c) already caught once. Probe runs get their own out dir, and a replaced CSV gets its verdict line re-compared, not trusted.
- A rounding change below the storage quantum is invisible at rung 1 and state-visible at rung 3: the normalize-once reciprocal disagrees with the two-division normalize on ~1.6 % of pairs (sampled), yet the l1 particle traces reproduced rev 1 bit-for-bit over 7,000 ticks — the grad/correction/writeback RNE layers absorbed every sub-quantum difference — while `--ladder 3` diverged at tick 20 because the residual carry stores exactly the bits the round discards. Test a kernel-rounding change against the ladder rung that can see it.
- A per-tick state hash compares ENCODINGS, not physics: retuning `omega_t`'s FRAC moved every body-scenario hash from tick 0 even where the motion is unchanged. When a row's format moves, expect full-trace deltas on any scenario whose arenas store that row, and judge those scenarios, never diff them.
- A CMake build dir bakes in the absolute path of the cmake.exe that configured it (VerifyGlobs re-runs it on every build); a removed portable CMake install poisons `cmake --build` even after the new CMake reconfigures — if configure itself keeps failing inside the old dir, delete the build dir rather than fighting the cache.
- A mechanism that exists twice has two failure paths: the isolate pool failed a broken waitpid loudly while the serial half decoded the same failure's zero-initialized status as PASS, and the driver's strict flag parser never reached the runner's identical atoll flags. When fixing a class, grep for its twin before calling it fixed (W1 wave-boundary sweep D1/D2).
- **A `World`-sized fixture on the stack is a Windows-only crash**: `comps[1024]` makes a World ~256 KB, a Windows child process gets a 1 MB stack (Linux: 8 MB), and one test held four fixtures - every Windows CI leg failed in suites that were green locally. Reproduce the constraint, don't guess at it: `ulimit -s 1024` found it in one run. Big fixtures live in statics (tests are exempt from the src/ static ban).
- **A test that violates the contract it tests can pass exactly where you run it.** The bytes hostile-length row passed n = 2^64-1 with a 1-byte out buffer against an API whose error path zero-fills all n bytes BY CONTRACT; dev -O1 happened to fold the doomed memset away (green locally) while debug/Windows/ASan all crashed. When a test probes a bound, first ask what the contract obligates the CALLER to provide at that bound.
- **The docs-commit gate is per-commit and per-module-directory.** A `src/foundation/` commit that documents itself in ECS.md (another module's doc) still needs `[docs:none]` - and a missing marker on an already-pushed commit means history surgery, because the per-commit walk re-runs on main forever after the merge. Check `commit_docs.py --base origin/main` locally before every push, not after.
- **When two states share every byte, the discriminator must be an out-of-band record, not another byte test.** A pending reused spawn reservation and a stale handle to a quarantined slot are both dead + generation-matching; `gen != GEN_MAX` looks like a fix and silently loses destroys for the 1/1023 last-generation case (a free-list slot legitimately sits at GEN_MAX). The window's popped-idx set - a transient record of what actually happened - is the exact separator.
- **Realize-order assumptions must survive the external chunk**: it RECORDS first (app/editor code runs between barriers) and APPLIES last (chunk id = system count), so "reservation order == apply order" is false for exactly one legal caller. Any barrier-applied invariant needs a trace with the external chunk in the interleaving before it ships.
- **"Commit and push every turn" reads two ways, and an autonomous lane picked the wrong one:** as a floor (never leave work unpushed) it is correct; as a ceiling (end the turn at each commit) it turns an unattended lane into a babysat one - the net-p1 lane stopped four times to "report status" nobody asked for. A rule written for sync discipline must say which direction it binds, and an autonomous brief must name its ONLY stop conditions explicitly (`WORKFLOW.md` §1 R-6); the commit trail IS the status report.
- **Second occurrence of the "trust the wave graph's dependency column" class (see the `PLATFORM.md` §9.5 / `VMemArena` line above), and this time it blocked a whole lane.** `ROADMAP.md` §2's net-p1 row names `TL_WIRE_STRUCT` and stops; `NETCODE.md` §20's implementation spec also needs `InputFrame`/`ActionState`/`MAX_ACTIONS` (`core/input.h`, a W3 lane's file) for the encoder, needs the `Replay` producer to close Phase 1's own done criterion, and calls `ByteWriter`/`ByteReader` - a pair named only in `ECS.md` §10.2 and defined in no doc at all. Before launching any lane, resolve every type its spec's structs and algorithms mention to an owning doc AND a file that exists on `main`; the ones no doc owns are the expensive find, because nothing fails until two lanes have each invented one.
- **A per-block header is free until you divide by the block size.** `NETCODE.md` §20.2.9's archive segment is a 112-byte header closing every `CHECKPOINT_HOT_TICKS` (300) ticks, plus an 8-byte stream header per (slot, channel). Over 30 minutes at 3 peers that is 360 segments: 39 KB of segment headers and 54 KB of stream headers around 31 KB of actual transition records - **75% framing**, against a < 80 KB gate. Taken literally ("for ch in 0..35") it is worse: 810 KB of stream headers alone at 8 peers, against a §13.4 target of 165 KB TOTAL, which is how the contradiction was spotted - the doc's own size table disproves the doc's own layout line. Two habits from it: when a format states both a per-block framing cost and a total-size target, divide one by the other before writing the encoder; and when a size gate fails, measure the split between framing and payload before touching the fixture, because a fixture that is already LIGHTER than the spec's own activity model (4,206 transitions/peer against a stated 5,000-8,000) cannot be the cause.
- **A fuzz round-trip only proves canonicality if the format has exactly one encoding per value, and "decode succeeded" is not that property.** T1f's `NETCODE.md` §20.6 wording - a mutated stream "is refused or decodes to something re-encodable identically" - failed on a mutation that CLEARED a `changed` bit: the stream still decoded, consumed fewer bytes than it was given, and re-encoded three bytes shorter. The fix was reading §20.2.2's rule as the biconditional it is written as ("bit a set <=> actions[a] != prev.actions[a]"), so a set bit decoding to the state the action already had is refused. Non-minimal varints and a value byte carrying the implied value are the same class. This matters beyond tidiness because §20.2.8 hashes archive bytes into `ChainEntry.log_segment_hash`: two encodings of one frame set fork the chain with no divergence behind it. Any format whose bytes are hashed needs its decoder to refuse every non-canonical spelling, and the test that finds them is mutation fuzz with a re-encode comparison, not a round-trip.
- **A test that reuses one stack buffer across cases must clear the bytes it does not write.** T0 fills every non-pad field of each wire struct from a table and leaves pads alone - correct, since a nonzero pad is what the refusal row tests. Reusing one `u8 src[256]` across 23 structs meant each struct's pads held the PREVIOUS struct's bytes, so the writer emitted nonzero pads and the reader correctly refused them: 14 failures that read exactly like a codec bug in the reader. Zero the row before filling it; more generally, a "fill the interesting fields" helper over a shared buffer is only correct if it also owns the uninteresting ones.
- **A round-trip test cannot see a format with two spellings; only a mutation fuzz with a RE-ENCODE comparison can.** The net-p1 archive shipped six T2 rows in which every byte was produced by the encoder under test and handed straight back to the decoder. Its adversarial review found four canonicality holes in an afternoon with a 40-line harness that mutated a payload byte, repaired the two crc32s, decoded, and re-encoded: an escape record could move the `down` bit that the action channel already carried; a slot could appear in `slot_mask` with no streams at all; channel 35 aliased onto the escape channel because `ARCHIVE_CH_COUNT` copied a doc typo; and a zero-tick segment encoded but could not decode. 8,677 of 128,319 mutated-but-decodable segments re-encoded to different bytes. The lane had ALREADY written the lesson ("the test that finds them is mutation fuzz with a re-encode comparison, not a round-trip") for the column codec and then did not apply it to the archive - the format whose bytes are actually hashed into the chain. Write the fuzz for every format you hash, and repair the checksums inside it or it only ever tests the checksum.
- **`TL_ASSERT` is not a precondition check; it is a dev-tier comment.** Three separate defects in one lane: locals used only inside an assert became unused-variable errors under `-Werror` on netcode/ship (11 of 23 CI legs red while dev and debug were green); the column encoder's "flags fit three bits" guard vanished on exactly the two tiers that ship, where `flags & 7` then truncates silently and every receiver applies a different frame than the sender; and the archive encoder's slot-ordering contract vanished the same way, where violating it produces a segment that is written, hashed into the chain, and then permanently unreadable. If violating a condition corrupts data or produces UB rather than merely indicating a bug, it is `TL_CHECK` - `bytes.h` already draws that line ("a producer that blows its own buffer is a bug ... TL_CHECK, all tiers") and the rest of the tree should match it.
- **An expansion loop that re-fills the tail is quadratic, and a per-block cap hides it until someone raises the cap.** The archive's transition decoder wrote `for (i = tick; i < tick_count; ++i)` per record instead of filling `[tick, next_tick)`. At `CHECKPOINT_HOT_TICKS` = 300 it is 0.14 ms and invisible; at 18,000 ticks it was 199 ms and at 60,000 ticks 4,557 ms, against 15 ms to encode the same segment - a 129x asymmetry an untrusted peer chooses the size of, since §20.2.5's `BK_LOG_SEGMENT` arrives over the wire. Fixing it to close the previous run at the next record's tick: 35 ms at 60,000 ticks. When a decoder's cost depends on a length the wire supplies, measure it at ten and a hundred times the current cap, not at the cap.
- **A revert experiment that does not check the BUILD reports false confidence.** Verifying that a regression test actually catches its defect means reverting the fix and watching the suite go red - but a revert that fails to compile leaves the previous binary in place, and the runner then reports the OLD build passing. Four "the test does not catch this" results in the net-p1 round-3 sweep were that artefact (`-Wunused-but-set-variable` on a now-dead local), and one genuine result was hidden among them. Any revert harness must fail loudly when the build fails, before it interprets a test result - the same rule as "an empty selection must be an error, not a clean run", one layer down.
- **A negative test can be refused for the wrong reason, and then it pins nothing.** Three of net-p1's new archive rows passed while the rule they named was reverted, because the forged segment tripped a DIFFERENT check first: an empty stream spliced out of (slot, channel) order was refused by the ascending-key rule, log records spread across ticks were refused by the effective_tick range rule when the tick count shrank, and a swapped escape pair tested ordering rather than the no-op refusal. When writing a test that asserts a REFUSAL, construct the forgery so that exactly one rule can object - and prove it by reverting that rule and watching the row go red. Otherwise the row is only evidence that something, somewhere, said no.
- **Named padding is written, so it must be initialized — and a zeroed stack hides the bug.** Shrinking the archive segment header left a 4-byte `_pad1` after `header_crc32` (the struct carries a `u64`, so it aligns to 8). The encoder never set it, so it put stack bytes on the wire, and the decoder correctly refused them as a nonzero pad — but only sometimes, because the first encode of a run usually lands on zeroed stack and the second does not. What caught it was the archive's mutation fuzz comparing a re-encode byte for byte: `diff@52, buf=00 again=FD`. A round-trip test would not have, since both halves of a round trip see the same garbage. Any `_padN` a wire struct declares is a field the writer owns; treat "explicitly padded" as "explicitly zeroed on every write", and let a re-encode comparison be what proves it.
- **A wire-format change retires the test corpus that guarded the old one, silently.** The net-p1 archive's option-A change (fixed 8-byte stream headers to two uvarints) left a hand-spliced forgery in the regression suite still writing the OLD header. Those bytes now parsed as several `record_count=0, key=0` headers, so the segment was still refused - by a different rule - and the row kept passing while the rule it named went untested: deleting that rule left all 60 rows green. Two habits: after changing a byte layout, re-run the revert experiment for every row that hand-writes bytes, not just the suite; and treat a passing negative test as unproven until the rule it names has been reverted under it.
- **A fuzz corpus can be overwhelmingly one shape and still look broad.** The archive mutation fuzz generated a million segments in which **97.2% of all streams were pointer streams and 22 in total were escape streams** - because the fixture toggled a button once per 64 ticks and the segments were 5 ticks long (0.23 toggles per segment), and because it constructed derived-consistent edge flags, which is exactly the case the escape channel does not carry. Two real second-spellings survived 10^6 cases for that reason. When a format has channel families, count what the corpus actually produced per family before trusting the case count; a big number is not coverage.
- **A steward window's context is spent by mechanical reads, not by thinking.** One orchestrator window burned most of its budget on three ~72 KB CI job listings, run listings that return full commit bodies for 25+ runs, and raw log tails — all read by the most expensive model. The cures are mechanical and are now `WORKFLOW.md` §6 R-10: SHA-targeted queries with `per_page` 1–3; parse every oversized tool result from its saved file with python instead of re-fetching; hand log harvesting to a cheap-model subagent that returns five lines; retire the window at a phase boundary — the committed-file handoff makes a fresh one cheap by design.
- **A status paragraph is the fastest-rotting doc surface and no gate can see it.** Root README, CLAUDE.md and docs/README all read "pre-code" two shipped waves later — docaudit checks references and constants, not prose claims about state. Status prose must carry its re-dated stamp, point at TODO/ROADMAP for the live truth instead of restating it, and be re-read at every wave boundary (`WORKFLOW.md` §3 artifact 4, R-12).
- **A shared-pool `live_bytes` delta is not an attributable witness when a vendored lib's allocator install is process-wide and permanent.** `SDL_SetMemoryFunctions` is never uninstalled, so any earlier row in the same process makes a later row's delta vacuous - a test can be load-bearing under `--isolate` and prove nothing in-process, and this repo runs the suite BOTH ways in CI (pr.yml build-test vs sanitize). Count at the adaptor, not at the shared pool (the FreeType call counter's shape) - took three review rounds to nail on the W2 vendor lane.
- **A red CI round is billed twice.** The lane pays a fix cycle and the steward pays an investigation, on separate sessions' budgets. Local validation before every push (`WORKFLOW.md` §6 R-11) is token policy, not just hygiene.
- **A gate ruling that names "a folder" needs TWO enforcement points, not one.** `PLATFORM.md` §9.5's "vendor_glue is the one folder allowed a static pool pointer" only holds if both `tools/audit/includes.py`'s regex-based `static` ban (gate 5) AND `tools/audit/symbols.py`'s `--data-only` `.data`/`.bss` link check are told about it — the second exists precisely to catch what the first's regex cannot (a non-`static`-keyword namespace-scope global), so patching only the grep leaves the real mutable global failing the link-level gate on the very first CI run. Reused RR-7's *stem*-scoped shape for the first fix and had to design a *whole-lib* shape for the second, because vendor_glue (unlike `src/foundation`) has no det/non-det split to key a stem list on.
- **A new module directory needs a `MODULE_DAG` entry before its first local include, not after.** `tools/audit/includes.py`'s `module_of()` derives a module name from any `src/<name>/` path automatically, but `MODULE_DAG.get(module, ())` defaults to the EMPTY tuple for a name with no entry — so `src/vendor_glue/x.cpp` including `"foundation/mem_pool.h"` fails the module-DAG gate with "violates the module DAG", not a missing-file error, until the directory is registered.
- **SDL3's own `option()`/`dep_option()` calls only see a cache variable set with `CACHE ... FORCE` before `add_subdirectory()`** — a plain `set()` (no `CACHE`) is silently overridden the moment SDL's `CMakeLists.txt` declares its own default, which is exactly why `vendor/CMakeLists.txt` forces `SDL_TESTS`/`SDL_EXAMPLES`/`SDL_INSTALL_DOCS` off with `CACHE BOOL "" FORCE` rather than a normal assignment.
- **A vendored sub-project's `find_package(OtherVendoredLib REQUIRED)` can resolve to an in-tree sibling with no install step**, if that sibling's own `CMakeLists.txt` calls `configure_package_config_file()`/`export(TARGETS ...)` unconditionally (not gated on its own INSTALL option, as SDL3's is not) — pointing `<Pkg>_DIR` at that sibling's build directory (`${CMAKE_CURRENT_BINARY_DIR}/sdl3` here) before `add_subdirectory(sdl_ttf)` is enough; no system SDL3, no `find_package` fallback logic needed. Check the producer's CMakeLists for that pattern before assuming a two-vendored-libs dependency needs a system package.
- **A vendored lib's own build script may `add_subdirectory()` ITS transitive dependency for you** — SDL_ttf's `CMakeLists.txt` adds `external/freetype` itself (with its own `FT_DISABLE_*` forcing) whenever `SDLTTF_VENDORED` is ON; adding it a second time from the parent `vendor/CMakeLists.txt` is a hard CMake error ("already added"). Read the consumer's CMakeLists for `add_subdirectory` calls over its own `external/`/`third_party/` folders before wiring a transitive vendor dependency yourself.
- **A vendored lib's CMakeLists.txt can define a working target with NO usable include path.** ENet's `add_library(enet STATIC ...)` never calls `target_include_directories` — it uses directory-scoped `include_directories(${PROJECT_SOURCE_DIR}/include)`, which only affects targets defined inside that same `add_subdirectory()` call and does not propagate to anything linking `enet` from outside it (unlike SDL3/SDL_ttf, which export modern `PUBLIC`/`INTERFACE` properties). The lib built clean; the first consumer TU outside `vendor/enet/` failed on `'enet/enet.h' file not found`. Add the missing `target_include_directories(enet PUBLIC ...)` from the parent `vendor/CMakeLists.txt` rather than assuming every vendored lib exports its own include path the modern way.
- **`project(tidelock LANGUAGES CXX)` had never actually enabled C — it was borrowed.** SDL3/ENet/FreeType all build `.c` files and all worked, but only because SDL3's own nested `project(SDL3 ... C)` call (the first vendor subdirectory processed) enabled C globally as a side effect, before anything else needed it. The first vendored lib with NO upstream CMake project of its own (Monocypher, `add_library(monocypher STATIC monocypher.c ...)` in a `CMakeLists.txt` this lane wrote) hit `CMake Error: Error required internal CMake variable not set ... CMAKE_C_COMPILE_OBJECT` at generate time — full C support (compile/archive rules) is only fully wired the first time a real `project()`/`enable_language()` call runs, not merely "identified". Fixed at the root: `project(tidelock LANGUAGES CXX C)`, with a C-compiler-is-Clang check mirroring the existing CXX one (`docs/BUILD.md` §1). A vendor tree with more than one C library should never depend on load order to make C usable.
- **"The base library is installed" is not "the feature is enabled."** `libx11-dev` alone left `SDL_X11` OFF and the same "could not find X11 or Wayland" fatal firing even after installing it on CI - `vendor/sdl3/cmake/sdlchecks.cmake` only sets `HAVE_X11` when it ALSO finds `X11/extensions/Xext.h` (`libx11-dev`'s `Xlib.h` is a different header from a different package, `libxext-dev`). Reproduced for real this time, not just read off the vendored CMake: installed CMake 4.4.2 via `pip install 'cmake>=4'` (this container's system CMake is 3.28, too old to see any of this), removed `libxext-dev` with `libx11-dev` still present, watched the exact fatal reproduce, reinstalled, watched a clean configure - then a full build and the smoke suite, under the real CMake 4 binary, before pushing. A dependency check that names the library you expect is not proof the library provides the SYMBOL/HEADER the build actually probes for; read the vendored detection script, don't infer requirements from a package name.
- **A local container's installed system libraries silently mask a vendor lib's real dependency.** SDL3's own CMakeLists.txt hard-fails configure on Linux when it finds neither X11 nor Wayland dev headers — five commits (SDL3 through Monocypher) all built and tested clean in this session's Linux container (which happens to carry X11 dev headers already) while every single CI job on all four `CANON.md` legs was red on all five, because the hosted CI runners carry neither. Windows CI failed too, but for an unrelated reason: FreeType's `builds/windows/ftsystem.c` and `builds/windows/ftdebug.c` (real Windows-specific source, not IDE-project cruft) were deleted along with the actual IDE-project junk when pruning `builds/windows/` wholesale during vendoring — invisible from a Linux container, since the Unix path (`builds/unix/ftsystem.c`) was untouched. Two lessons: (1) a vendor lib's own upstream CMakeLists.txt is a more reliable prune-safety oracle than "which directory names look like IDE cruft" — grep it for every path it references before deleting a directory wholesale; (2) **local validation on one OS proves nothing about a build gate the local OS never exercises** — CI's four-leg matrix exists precisely because this container cannot BE all four legs, so a red multi-commit CI run after several "verified locally" pushes is the system working as designed, not a lane failure, but it is still exactly the class of mistake `LESSONS.md` already warns about ("Check CI before writing 'verified' in a commit message") — the fix there was to check CI status once per push, not to trust a green local run across five.
- **A vendored lib's `cmake_minimum_required()` floor is invisible to local validation whenever the local CMake predates the cutoff it will actually hit.** CMake >= 4.0 hard-errors configuring any tree whose floor is below 3.5 (removed outright, not deprecated) - ENet's is `2.8.12...3.20` and FreeType's is `3.0...3.10`, both below the cutoff, both silently accepted by this container's CMake 3.28. Caught by checking every vendored tree's `cmake_minimum_required()` line proactively (`grep -rn cmake_minimum_required vendor/*/CMakeLists.txt`) after a report that CI's hosted runners now default to CMake 4.x - not by a failure this container could reproduce. Fixed with CMake's own migration knob, `CMAKE_POLICY_VERSION_MINIMUM 3.5`, set once as a CACHE variable before any `add_subdirectory()` in `vendor/CMakeLists.txt` (a cache variable reaches every subdirectory configured afterward, including FreeType's nested `add_subdirectory()` inside SDL_ttf's own CMakeLists.txt) - never by hand-editing a vendored `cmake_minimum_required()` line, which would violate "vendored verbatim" for a problem CMake already has a sanctioned answer to.
- **A vendored lib's own CMakeLists.txt may special-case the wrong Windows toolchain family.** ENet's `if (MINGW) target_link_libraries(enet winmm ws2_32) endif()` links the Winsock/multimedia timer import libs ONLY for MinGW — `win32.c`'s `WSAStartup`/`socket`/`timeGetTime` calls are exactly as undefined under clang-cl (this tree's Windows compiler, `docs/BUILD.md` §1) as under any compiler with no `ws2_32.lib`/`winmm.lib` on the link line, but `MINGW` is false for clang-cl, so the condition silently misses it. Found by reading the CMakeLists ahead of a CI round that had not yet reached ENet's link step (SDL_ttf/FreeType's configure-time breaks came first) — added the missing `if(WIN32 AND NOT MINGW)` link from the parent `vendor/CMakeLists.txt` rather than hand-patching vendored code. A narrow upstream `if(MINGW)`/`if(MSVC)` guard is worth checking against clang-cl specifically: it satisfies neither macro by the name a maintainer who never tested clang-cl would reach for.
- **A header-only vendor lib's "one implementation TU" cannot compile under `tl_flags_common`.** stb_image.h/stb_sprintf.h's `STB_*_IMPLEMENTATION` bodies were never written to satisfy `-Wsign-conversion -Werror` (dozens of `int`/`size_t` implicit conversions) — instantiating them inside a `src/vendor_glue/*.cpp` TU (which links `tl_flags_common`, our strict subset flags) fails to compile with ~20 errors on the first file alone. "Vendored libs are exempt in their own TUs" (`CLAUDE.md`) only holds if the TU compiles under the VENDOR's flags, not ours — for a header-only lib with no upstream `.c`, that means writing a tiny `.c`/`.cpp` of our own INSIDE `vendor/<lib>/` (its own `CMakeLists.txt`, no `tl_flags_common`) to hold the `#define STB_..._IMPLEMENTATION` + `#include`, with only thin `extern`-declared hook functions crossing into `src/vendor_glue/` where the real (strict-flags) pool-hookup logic lives. Declaration-only includes of the same header (no `IMPLEMENTATION` macro) are just prototypes and compile clean under strict flags, so `src/vendor_glue/stb_glue.cpp` can safely `#include <stb_image.h>` for that half.
- **"Allocator hooked to the pool" is not met just because the top-level vendored lib's own hook API got called — a transitively vendored dependency can have its own, unhooked allocator underneath, and only a measured test catches it.** SDL_ttf's `TTF_Init()` calls `FT_Init_FreeType()` with no argument for a custom `FT_Memory`, so FreeType (vendored under `vendor/sdl_ttf/external/freetype/`) allocated through plain `malloc`/`realloc`/`free` the entire time `sdl_ttf_glue.test.cpp` was asserting `TTF_Init()` returned true — a test that only checks the call succeeded passes identically whether or not the allocator underneath is hooked (`docs/TESTING.md` §7 "measure, don't assert" exists for exactly this). Caught by round-1 adversarial review, not by the six vendor_glue tests that shipped alongside it; confirmed for real (not just plausible) by reverting the eventual fix and watching a hard crash (`pool_free` choking on a foreign libc pointer) rather than trusting the review's claim. The general form: a wrapping lib's public init function is not proof every allocation underneath it reached the pool — check whether ITS dependencies have their own allocator-registration story, and if the wrapper's init gives no way to inject one, look for the dependency's own platform-customization seam (FreeType's is `builds/<platform>/ftsystem.c`, split out from `src/` for exactly this kind of per-platform override) rather than assuming "no seam" means "exempt".
- **A vendored C++ library is only "pooled" if every part of it has a hook, and Luau's does not.** `MEMORY.md` §2's premise is that vendor libs route through `mem_pool` via their hook APIs; measured at the 0.696 pin, the Luau **VM** does exactly that (`luau_load` + `lua_pcall`: **zero** global `operator new`), and the Luau **Compiler** — which has no hook API at all — makes **32** per `luau_compile`. The dev/netcode alloc-shim tripwire turns that into a `TL_FATAL`, so an in-process compile kills the suite in the two tiers the done criterion names, while `debug` and `ship` (no tripwire) stay green and hide it. Check the hook coverage of every COMPONENT of a vendored library, not of the library.
- **A context-free C callback cannot reach engine state under a writable-static ban.** Luau's `useratom` is `int16_t(*)(const char*, size_t)` — no `lua_State*`, no userdata — so the specified "return the interner's `StrId`" needs a namespace-scope pointer that `CPP-SUBSET.md` §1 bans. Third instance of the class (the CRT-malloc counter, `vendor_glue`'s static pool pointer, this), which is the argument for one general mechanism rather than three exceptions. Read the CALLBACK SIGNATURE before speccing what the callback consults.
- **A gate token written before the thing it gates is vendored fires on the wrong file and never on the right one.** `BACKEND_HEADERS`'s bare `"luau"` predated the vendoring: upstream spells its headers `<lua.h>`/`<lualib.h>`/`<luacode.h>` and `Luau/*.h`, so the token matched no real include — while it DID match our own `#include "vendor_glue/luau_alloc.h"`. Re-derive a speculative gate entry against the real tree the day it lands.
- **`luaL_checklstring` makes stock `%s` type-error on a table; Luau's `%*` extension prints the address.** The sandbox's `string.format` wrapper has to intercept `%*`, which no doc named, more than `%s`, which every doc did. `%p` needs nothing — Luau rejects it as an invalid option. Read the vendored implementation before deciding which conversion leaks.
- **The runtime's only sort is an integer radix with no comparator.** `CONTAINERS.md` §4's `sort_u32_kv`/`sort_u64_kv` cannot express "numbers before strings, then bytewise", and §6 puts the only comparison sort in `tools/`. A spec that says "sorts with the foundation sort" over mixed keys is naming a function that cannot do the job; the caller carries its own stable merge.
- **An oracle that asserts its own preconditions cannot be asked whether it is in range.** `fx::div<q_t>(a, b)` traps when the quotient leaves `q_t`; computing `want = div(...)` and *then* range-checking `want` traps before the check runs. Compute the raw quotient with `rne_div` first and sweep only the in-range pairs — the same "generate inside the contract" rule, applied to the ORACLE rather than to the generator.
- **A negative fixture built on a base layer that already violates something passes on the wrong violation.** Four new `symbols.py` rows used the `lower` fixture, which carries a deliberate upward reference, so every one of them saw `rc == 1` for a reason unrelated to the plant. Build a negative row on a base that is otherwise clean, and assert the MESSAGE names the planted symbol.
- **A lane brief's environment claim is a hypothesis, not a fact.** This lane's brief said "`netcode`/`ship` refuse configure" in the container, so the first two pushes were validated on `dev`+`debug` only — and the netcode/ship `-Werror` unused-parameter break (the `#if TL_DEV` body class already in this file) took eight CI legs red. `-DTL_STRICT_TOOLCHAIN=OFF` configures all four presets here. Try the thing the brief says is impossible before scoping local validation around it; a red CI round is billed twice (`WORKFLOW.md` §6 R-11).
- **`operator new` is replaceable per PROGRAM and nowhere narrower — so a vendored library that allocates with it forces a program-wide decision.** The cure that kept every existing rule: split the tripwire operators into their own archive member so ordinary archive semantics let a replacement win with no duplicate symbol (they had shared a member with the anchor the guard force-pulls), then open a bounded WINDOW around the one call that needs it and charge the bytes to the pool that call is for. Outside the window the replacement fatals exactly like the tripwire, so the ban is unchanged where it was doing work.
- **A per-test copy of a process-stable resource trips the guard that protects it.** `StrId` is process-stable by CANON, so `script_install_useratom` fatals on a second, different `Interner` — and a fixture that built one per test hit it on test two. The fixture was wrong, not the guard: a shared process interner in a function-local static is what the real program has.
- **Both halves of a two-key exemption need their own planted violation.** `tools/audit/static_allow.txt` is keyed by (lib, directory, stem); the fixtures plant the exempt directory at a different stem AND the exempt stem in a different directory, for each row. RR-7's original defect was exactly a stem-only match, and one fixture per row would have re-introduced it in the new file.
- **Reason about a link line by READING it, never by reasoning about what CMake "must" do.** The RR-18 `operator new` replacement shipped with a comment claiming CMake orders `tl_vendor_glue` before `tl_foundation` because of the dependency; the generated `build.ninja` lists `libtl_foundation.a` FIRST for `tl_tests` and puts `libluau_compiler.a` *before* `libtl_vendor_glue.a` for `tidelock`. It works for a different reason (`vm.cpp` references `vendor_heap_install`, so the member is pulled while vendor_glue is scanned), and the wrong stated reason is what would have rotted. Print `LINK_LIBRARIES` out of the generator before writing the sentence.
- **When one failure mode is a loud link error and the other is a silent fallback, spend the test on the silent one.** Both `operator new` definitions in one binary is a duplicate-symbol error on every leg; the CRT quietly serving the compiler instead of the pool looks identical to success. The live-byte assert around the compile window cannot see it (net zero either way) — the pool's PEAK can, and with a measured magnitude floor (112 KB observed, 16 KB asserted) rather than a bare `>`, which one incidental block would satisfy.
- **A build-time projection from one machine to another is an estimate, and estimates get refuted.** This lane measured Luau's cost on the container (9.04 s → 13.39 s, +48 %) and projected ~17.9 s on the CI leg, "1.4x headroom". The leg measured **22.66 s against a 25.00 s budget — 1.10x**, and the same gate has already gone red at 25.27 s on a docs-only commit against a smaller tree. Scale a timing budget from the leg that grades it, never from the box you happen to be on (`CLAUDE.md` rule 4).
- **A guard measured across a wider operation than the one under test cannot discriminate, however careful its floor.** The RR-18 row asserted the pool peak moved across `script_run_source` — which compiles AND loads AND runs, and only the first goes through `operator new`. The compiler was 28.8 % of the delta, so the "conservative" 16 KB floor sat a factor of three below `luau_load`'s share alone and the row passed with the CRT serving the compiler. Measure the window, not the call that contains it; if the window has no observable, give it one (`script_last_compile_bytes`) rather than widening the assertion.
- **Two allocators with opposite failure semantics must not share one budget.** `tl_luau_alloc` returns null over budget and Luau makes that a recoverable error — a test pins it as the contract — while the `operator new` replacement on the *same pool* `TL_FATAL`s. A 512 KB VM compiling a 200 KB source killed the process in every tier including `ship`, and the trip point depended on runtime heap occupancy rather than on the input. Before putting a second allocator on a pool, ask what each does when the budget says no.
- **Reimplementing a vendored function "around" it drops the step you did not know was there.** The sim VM's freeze hand-rolled two of `luaL_sandbox()`'s three steps and narrowed the first to a two-name list — losing the string metatable, a table permanently rooted by the VM and writable from a *sealed* script, where a field then survived ten tick brackets. Read the vendored function you are about to replace; if you keep only some of it, say which step you dropped and why.
- **A test that asserts a thing EXISTS can bless a hole.** `assert(math ~= nil)` pinned the data VM's `math` table as intended — including `math.random`, seeded from an address and the wall clock, in a VM whose output is hashed. An existence assertion is only as good as its granularity: name the members the contract is actually about.
- **A symbol gate that matches raw names is blind to the other linkage.** The `lua_*`/`luaL_*`/`luau_*` prefixes cover Luau's `extern "C"` surface and none of its 8,483 mangled `Luau::` symbols; a hand-written C++-linkage extern taking a `lua_State*` passed with 0 violations. Match the demangled name too — by word boundary, not prefix, because MSVC demangling puts the return type and calling convention first — and write one fixture per LINKAGE FAMILY, not one per rule.
- **A ruling that amends four docs can still miss the header that IS the contract.** RR-18/RR-19 updated `MEMORY.md`, `CPP-SUBSET.md`, `PLATFORM.md` and `LUAU-LAYER.md`, and left `script.h`'s contract block stating the opposite of what the module does — the first thing a downstream lane reads (`CPP-SUBSET.md` §6). When a ruling changes behaviour, grep the shipped headers for the claim before the docs.
- **A capability function that can only return one value is not a fact, it is dead code.** `script_can_compile_in_process()` and its `ERR_SCRIPT_NO_COMPILER` survived the ruling that made them constant. Delete the branch with the condition; a queryable capability earns its keep only while something can vary.
- **A lifetime high-water mark cannot answer a per-operation question.** `mem_pool`'s `peak_bytes` is `max(live_bytes)` EVER, so a compile window that returns every byte it took raised it once and never again: the meter read 32,992 / 0 / 0 across three compiles on one pool, and on a long-lived shared pool it reads 0 essentially always. The fix was not in the test — the counter has to live where the allocations are intercepted, so it can be reset per window. Before deriving a per-operation figure from a pool statistic, ask whether that statistic is cumulative.
- **A refusal gate must measure the quantity the enforcer enforces.** The compile-headroom check computed `budget - live`; `pool_alloc` refuses on `carved_bytes + bytes > budget_bytes`, and `pool_free` returns a small-class block to its freelist WITHOUT decrementing `carved_bytes`. So an aged pool reported 33.6 MB of headroom against 40 KB of real carve room and the process died anyway. `budget - carved` ignores freelist reuse, which is the correct direction for a refusal: it can refuse something that might have fit, never admit something that cannot.
- **A test that branches on the outcome passes under both the fix and the defect.** My first aged-pool row asserted "refused if the room is short, succeeds otherwise" — true of the broken check too, so it verified nothing. Age the fixture until the expected outcome is UNCONDITIONAL, and assert that one outcome; if the setup cannot reach it, the row is not ready.
- **An exact count beats a floor whose stated property is wrong.** The sweep's floor claimed eleven unconditional comparisons per pass; there are ten, so it sat 705 comparisons below the real figure and an entire op's 256 could vanish untripped — with a broken op it still cleared the floor. Counting attempts alongside successes (`checked == attempted`) is exact, needs no arithmetic about which rows are conditional, and caught a broken CONDITIONAL op that no floor could have.
- **`git checkout <file>` to undo a scratch edit also undoes the real edits in that file.** Used to revert a revert-experiment, it silently discarded a session's worth of test work; the src/ changes survived only because they were in other files. Copy the file aside before a scratch edit and copy it back, or make the experiment in a file you are not currently editing.
- **A PR with a merge conflict gets NO CI run at all — not a failing one, not a queued one.** `pull_request` events build `refs/pull/N/merge`, which cannot exist while the merge is dirty, so GitHub silently creates no workflow run for the push. Two pushes to `w2-luau-vm` produced nothing while `main` had moved ahead, and a poll-for-the-run watcher would have spun forever: the symptom is indistinguishable from a slow queue. Before waiting on CI, check `mergeable_state` — `dirty` means merge the base branch first, and the run appears only after. `workflow_dispatch` (which `pr.yml` carries for exactly this) proves a branch green when the event path is not firing.
- **A ruling that closes a nondeterminism channel closes it for ONE door unless you sweep the others.** D4 removed `math.random` from the data VM because its output is hashed; the same VM kept stock `tostring`, whose default case prints the object pointer — the same class, same VM, same consequence (a fingerprint mismatch on handshake instead of an error at the line), found three rounds later by reading the tree whole. The round-1 sweep that would have caught it was scoped to the sim VM. After ruling a channel shut, re-run the sweep in EVERY VM the rationale applies to, not the one the finding arrived from.
- **A "removal list" and a "replacement" are different mechanisms and a constants sheet must not blur them.** `CANON.md` listed `tostring`/`%p` among removed globals as "replaced"; `%p` needs no replacement (Luau rejects it) and the conversion that leaks is `%*`, which appeared in no contract doc at all — only in code, a test, and a LESSONS line. A doc that names the wrong mechanism is worse than one that is silent: a reimplementation from it drops exactly the protection that mattered.
- **`SystemFn`'s `void(*)(World*)` shape has no path to `Engine`-level state, and the gap shows up on the third thing that needs it, not the first.** w3-loop-input hit it three times in one slice: the interpolation ping-pong, the LAST-phase recorder (spec'd as a registered system, built as a direct call from `engine_tick_once` instead), and `PRE_RENDER`'s `alpha`, which `engine_frame` can only hand back to its own caller. Filed as one ruling request (`TODO.md` RR-25) rather than three, once the pattern was visible — a future consumer designing against `SystemFn` should know the ceiling before hitting it a fourth time.
- **A duplicate constant across two modules that don't currently share a TU is not "harmless" — it is untested, not unbroken.** `core/input.h` and `net/wire.h` each declared their own `constexpr u32 MAX_PEERS = 8u;`; nothing in the merged tree included both headers in one TU, so it compiled clean and TODO.md's RR-24 called it "harmless today". A fresh-context review actually included both and hit `error: redefinition of 'MAX_PEERS'` — CPP-SUBSET.md's own global-namespace-no-per-module-wrapper ruling names "revisit only on a real collision" as the trigger, and "no TU currently exercises the collision" is not the same claim as "no collision exists". Ruled 2026-08-26 (Rafael): `core/input.h` is the one C++ home (CANON.md names no header, so the seam's consumer-facing side owns it) and `net/wire.h` gets a scoped exception — delete its copy, include `core/input.h`, keep its own `static_assert`. (A same-day first attempt introduced a new shared `foundation/net_limits.h` header instead of asking which existing module should own it — reverted once the ruling named the actual owner; a real collision is worth a ruling request before inventing a third home.)
- **A closed token-keyed table can be closed against the very kind it names.** `core/reflect.h`'s `FieldKind` enum lists `K_f32`-shaped nothing at all — every render-side "component" `docs/RENDER2D.md` §9.2 pins (`Camera2D`/`CameraPrev`/`CameraFollow`) is f32-fielded, and `TL_COMPONENT`/`TL_FIELDS_Name` cannot reflect a kind with no `tl_field_kind_<token>` constant — a compile error only at the macro expansion, not at the enum. A second instance one file over: `tl_field_kind_TexHandle` doesn't exist either, and the doc's own instruction to add it "beside the type definition" is unsatisfiable — `TexHandle` lives in `platform/platform.h`, and `tools/audit/includes.py`'s `MODULE_DAG` bars `platform` from including `core` (the dependency runs the other way), so the constant cannot physically sit next to the typedef. Both were resolved locally (an empty-field-table `ComponentInfo` for the float structs; the `TexHandle` constant landed in the first module that can see both `core` and `platform`) and filed as ruling requests (`TODO.md`, RR-23/RR-22 — RR-23 renumbered from a same-day collision with `w3-assets-data`'s own RR-21) rather than silently worked around — a closed kind set is a promise every consumer can rely on the enum being complete, and it wasn't for the first render-side reflected struct that used it. **Update (2026-08-27, review round 1 D1):** the empty-field `ComponentInfo` workaround for `Camera2D`/`CameraPrev`/`CameraFollow` is gone — a byte-level hash (`registry_hash_all`) doesn't consult the field table an empty one hides a struct from, so it never actually solved the problem it was filed for; the camera came off the ECS entirely instead (`RENDER2D.md` §2 R-3). The `K_f32` gap this entry names stays open for whoever hits it next; `TexHandle`'s fix is unaffected.
- **`TL_ASSERT(expr)` wrapping a call directly deletes the call, not just the check, at `TL_DEV=0`.** `TL_ASSERT(c)` expands to the literal text `((void)0)` in netcode/ship — the macro parameter `c` never appears in that arm's body — so a preprocessor that never evaluates anything (it substitutes text) drops whatever expression was passed, including a function call with real side effects: three render2d test fixtures wrote `TL_ASSERT(scratch_init(...) == ERR_OK);` with no intermediate variable, and on netcode/ship `scratch_init`/`world_init`/`vmem_arena_init` were never called at all, leaving the fixture's `World`/`Scratch` uninitialized underneath tests that looked green in `dev` builds. A sibling shape (`ErrCode e = call(...); TL_ASSERT(e == ERR_OK);`) is the milder, already-documented `-Wunused-but-set-variable` version of the same class (this file, "`-Wunused-parameter` on a compiled-out local"). Neither is a test-fixture problem specifically — it is what `TL_ASSERT` always does at `TL_DEV=0` — but a fixture `init()` helper is exactly where the missing call is invisible until the assertion that would have caught the corruption is *also* the thing deleted. Fix: a fixture init helper returns `ErrCode`/`bool` (the `jobs_test_util.h`/`world_test_util.h` idiom already in the tree) and the TL_TEST body checks it with `TL_ASSERT_EQ`, which is tier-live in every tier because it goes through the runner's own `tl_test_fail`, not the compiled-out assert. Caught by CI red on netcode/ship + rebuild-budget + fingerprint-stability (all four collateral from one root cause, since they all build the netcode preset) — `dev`/`debug`-only local validation cannot see this class; a netcode-tier local build (`-DTL_TIER=netcode -DTL_STRICT_TOOLCHAIN=OFF`, in-container) can and now does, per `WORKFLOW.md` §6 R-11.
- **A commit's `commit_docs.py` obligation is fixed the instant it is written; no later commit — revert included — can discharge it.** `tools/audit/commit_docs.py` walks `base..HEAD` and checks each commit's OWN `git show --name-only` diff independently (its own selftest: "a later `[docs:none]` does not waive an earlier commit"). Round 2's N5 fix landed a cross-module edit (`src/foundation/fx_float.h`, not this lane's cone) with no doc touch or `[docs:none]`; the very next commit reverted that edit byte-identical to `main`, and `commit_docs.py --base origin/main` still failed, still naming the ORIGINAL commit's SHA — reverting removes the fact from the TREE, not from that commit's DIFF, which is immutable once written (RR-25/RR-26, `docs/RENDER2D.md`, `TODO.md`). The only cure once review has begun (history frozen, `WORKFLOW.md` §1) is a message-only reword of that specific commit by whoever holds the permissions to do it — never "I'll add `[docs:none]` next time." The cheap defence: run `commit_docs.py --base origin/main` before every push, not after — it reproduces CI exactly, and a local pass there is the only thing that actually prevents this, not detects it.
- **A maintained count in prose is a bug that hasn't happened yet, and correcting its value is not the fix.** One `TODO.md` sentence stating "how many of the round-1 findings have a discriminating test" was wrong four separate times in a row across four review rounds on the render2d lane — "20", then "15" (tree already had 16 both times), then "17 after D1, 22 after round 2" (the commit that wrote "22" already had 33, per `git blame` — wrong the moment it was written, not merely stale by the next commit), then an arithmetically-impossible "fifteen … only two do not" (16 − 2 = 14, and the sentence's own parenthetical enumerated ten) written in the SAME commit that was supposed to fix the third error. Each round corrected the number; each correction produced a new wrong number, because the fix kept being "write a better count" rather than "stop maintaining one." The actual fix: delete the number. Name the categories instead ("every finding with a runtime observable has a test, except X for reason A and Y for reason B") and point at the command that derives the true count (`tl_tests --tag render`'s own "N selected" line) rather than a value a human must keep in sync with a tree that changes every commit. Applies to any record — a doc, `TODO.md`, a PR body, a commit message — not just this one row; sweep for other bare counts of the same shape (test/file/commit totals presented as an ongoing truth, not a dated measurement of one specific past run) whenever this class of error is found once.
- **A cloud child session inherits the environment but NOT the repo — and a lane briefed without one is right to refuse.** `create_session` without `source_url`/`source_revision` produced three W3 lanes whose containers held no checkout; each was handed a brief telling it to push under Rafael's identity to a repo it could not see, and all three stopped and asked instead of proceeding. That refusal was correct behaviour, not a malfunction: the brief's instructions arrived without any of the evidence that justifies them. Register the source on every spawn, and open every brief with a provenance preamble that names who wrote it and points at the committed files (`CLAUDE.md`, `WORKFLOW.md`, `ROADMAP.md` §2, the `TODO.md` ruling) that independently justify each instruction, with "where this brief and a committed doc disagree, the doc wins."
- **The poke channel cannot reach a lane mid-turn, and `WORKFLOW.md` §1 R-6 makes lane turns long by design.** A CI diagnosis sat unread in a lane's queue while it stacked three more feature commits on the broken head, each burning a 23-check run. The only way to steer a running lane is `interrupt_session` then re-deliver — and the interrupt's control payload carries `cancel_queued: true`, so assume the queued message died and send it again. Cheaper still: brief lanes to check their own PR's CI at each commit boundary, since they are already pushing there.
- **A `workflow_dispatch` re-run of `pr.yml` reports a FALSE green.** The workflow computes `commit_docs.py --base` as `github.event.pull_request.base.sha || github.event.before || 'HEAD~1'`; a dispatch run has neither of the first two, so it silently falls back to `HEAD~1` and validates one commit's diff instead of the whole PR range. A lane hit slow webhooks, fired the workflow by hand, and reported 23/23 green on a head whose real `pull_request` run failed `audits` for four commits. Only a run whose `event` is `pull_request` counts as evidence; verify the event, not just the conclusion.
- **"Revert-verified" from the party that wrote the fix is a claim, not evidence — make the reviewer re-run it.** One lane reported all sixteen of its fixes revert-verified in the words "reverted, rebuilt, confirmed the expected failure, restored". Re-run independently: ten discriminated, and one was provably impossible to verify (`to_f32(TEXEL)` and `1.0f/16.0f` are the same `0.0625f`, so no revert can produce a failure). The fix that worked was changing what counts as a report: the command AND the real failure output, per item, or the item is treated as unverified regardless of the commit message. An honest "cannot discriminate, because X" is worth more than a "verified" that has to be re-checked.
- **A hand-derived count is the wrong denominator even when a careful reviewer derives it.** Reviewing a record that claimed 33 tests, an Opus reviewer grepped `^TL_TEST(`, got 27, and was one step from filing it as a wrong count — until it noticed `TL_TEST_EXPECT_FATAL` is also registered by the runner. 33 was right and the grep was wrong. This is the maintained-count lesson from the other side: point at the runner's own "N selected" line rather than any count a human or a reviewer computes by hand.
- **Concurrent lanes must not self-allocate from a shared ruling-request counter.** Three W3 lanes filed `RR-21` within about thirty minutes of each other, and one of those numbers was already ruled and cited in shipped code comments. The steward had to renumber two lanes retroactively. Either the steward allocates ids, or they are lane-prefixed (`RR-r2d-1`) — a global counter with concurrent writers is a collision generator.
- **An IDLE lane in `review_ready` is waiting on the steward, not working.** Steward attention follows the loudest lane: while one PR consumed five review rounds, a second sat green and review-ready for ten hours with no reviewer spawned, and a third sat idle waiting for a history rewrite only the steward could perform. Sweep every lane on a timer, and read `get_session`'s status as an instruction — `review_ready` + IDLE means the queue is blocked on you.
- **`--force-with-lease=<branch>:<sha>` is the difference between a safe rewrite and clobbering a lane.** A steward-executed reword was rejected mid-push with "stale info" because the lane had pushed a new commit while the rewrite ran; the lease refused rather than discarding it, and the fix was to re-fetch, redo the rewrite on the new head, and push again. Always pin the expected SHA explicitly — a bare `--force`, or a lease without the SHA, would have silently destroyed that commit.
- **GitHub registers a merge and auto-deletes the head branch even when the merge arrives as a plain push.** Pushing a `--no-ff` merge commit to `main` (rather than clicking Merge) still flipped the PR to `merged: true` with the correct `merge_commit_sha`, and the repo's auto-delete setting removed the branch unprompted. Merging locally therefore costs no audit trail and needs no manual branch cleanup — do not ask Rafael for a tap that the merge already performed.
- **Archive a child session the moment its work is filed, not "at the closeout" — the steward will not get there.** After PR #13 merged, its lane session and all five review sessions were still open, plus two reviewers on other lanes whose verdicts were already posted: eight live sessions for work that was finished, found only because Rafael looked at the session list. Archiving had been promised twice in-session and deferred both times behind whatever was loudest. A review session's job ends when its verdict is on the PR (the PR comment is the durable record, not the session); a lane's ends when its branch merges. Archive at that instant, in the same turn as the merge or the verdict relay — treat a finished-but-open session as a defect in the steward's own queue, since every one of them is a container still allocated and a status line that lies about what is in flight.
- **A child session cannot notify the steward, so never build a workflow on it being notified — make completion OBSERVABLE and poll for it.** Three stalls across two windows had one root cause and it is not laziness in the children. There is no working child→steward message channel in the cloud environment: `SendMessage` reaches only same-machine sessions (`ListAgents` returns "no reachable agents"), and the remote MCP server exposes `create_session`/`interrupt_session`/`create_trigger` but no send. Briefs nevertheless told reviewers to "post the verdict, then poke the steward" — so each one posted, believed it had reported, and went IDLE; a fresh-context session has no way to discover that the tool it was told to use does not exist. The steward then made it worse in the honest direction, telling both lanes *"round 3 follows; I spawn it, you do not need to ask for it"* — a sentence that converts a finished, green, mergeable lane into a silent one **by design**. Two lanes then sat green with 23/23 CI and `mergeable_state: clean`, politely re-arming 30-minute self-check-ins that only re-confirmed they were still green, until Rafael read their transcripts and asked. The fix is not a better instruction to the child; it is to stop needing one. Two signals are observable without any cooperation from the child and cannot be forgotten by it: **a branch head moving** (`git ls-remote`, free, no API budget) and **a PR comment appearing** (one `issues/comments?sort=created&direction=desc` call covers the whole repo, so a 2-minute cycle stays far under the 60/hour unauthenticated limit). Run both as a background `Monitor` whose stdout lines wake the steward, and write every child brief to say **"your PR comment / your push is the ONLY signal that you are done; do not try to message anyone"** — which is also true, and stops the child inventing a channel. Corollary for lane briefs: `WORKFLOW.md` §1 R-6 tells a lane not to stop at commit boundaries to report status; that rule is about not interrupting *its own* work, and it is not a licence for the steward to promise a follow-up the lane cannot verify ever arrived. State what the lane should do if the next round has not started within a bounded time — push or comment, never wait silently.
- **R-15's watch is capped at 30 minutes even with `persistent: true` — the timeout notice is the re-arm signal, so treat it as one.** The steward's PR watch was started with `persistent: true` and `timeout_ms: 3600000`; the tool accepted it and reported `timeout 1800000ms`, then fired `[Monitor timed out — re-arm if needed.]` half an hour later. A rule whose whole point is "the steward is woken instead of polling" fails silently if its watch quietly expires — and it expires during exactly the long reviews it exists to cover. The saving grace is that the timeout notice itself wakes the session, so re-arming on every such notice is self-sustaining and costs one call; what is NOT safe is assuming `persistent` means session-length. Re-arm immediately on the notice, before anything else in the turn, and keep the long `send_later` backstop whose job is the one thing no watch can see — a child that died without ever pushing or commenting.
- **A conflicted PR gets NO CI at all — absent, not red — so "zero check runs" means check `mergeable_state` before suspecting the CI trigger.** `w3-loop-input` pushed `3061af2` and the `check-runs` API returned `total_count: 0` with no `pull_request` workflow run created; the branch's newest run was still the previous head's. That reads exactly like a broken webhook or a workflow-trigger regression, and the natural next move — re-running by hand — is the `workflow_dispatch` FALSE-GREEN trap this file already documents, so a misdiagnosis here costs a fake green rather than a red. The actual cause is that `pull_request` workflows build the merge ref, and GitHub cannot create one for a PR whose `mergeable_state` is `dirty`, so no run is created at all. Confirmed by a clean before/after on one branch: dirty at `3061af2` → zero runs; the conflict resolved by merging `main` at `dd11b38` → a `pull_request` run created immediately with the full check set. Read the two facts together — absent CI plus `dirty` is one event, not two — and note the ordering it forces on a steward: **merging one lane can silently stop CI on every other open PR that touches the same files**, and the affected lane will not be told; the steward has to notice the `dirty` and dispatch the merge.
- **A macro's for-loop control variable needs a name unique per expansion site, or the macro cannot nest — and nothing catches that until the first nested call site is actually compiled.** `TL_PROF_SCOPE(lit)` (W1 tooling-rt, `foundation/tl_prof.h`) expanded to `for (u32 _tl_ps = ...; ...)` with a fixed name; two `TL_PROF_SCOPE` calls nested inside one another (exactly the profiler's reason to exist — a hierarchical scope tree) redeclare `_tl_ps` in the same block scope and fail `-Wshadow -Werror`. No call site had ever instantiated the macro before `w3-editor`'s first test wrote one — another instance of "a template with no call site has never been compiled" (this file, above), this time for a macro rather than a function template. Fixed with the standard two-layer token-paste idiom keyed on `__COUNTER__` (`TL_PROF_CONCAT(a,b)` expanding through an indirection so `__COUNTER__` is evaluated exactly once per macro invocation, not once per reference to the pasted name inside the body — referencing `__COUNTER__` directly three times in one macro body would itself produce three DIFFERENT names). Any `for`/`do-while` scope-guard macro meant to nest needs this from the day it is written, not from the day someone nests it.
- **An `inline` function in a header that touches an anonymous-namespace variable is an ODR violation the moment a SECOND `.cpp` file includes that header — and it can pass on one tier and abort on another.** `tests/editor/imgui_test_util.h` declared `ImGuiContext* g_imgui_test_ctx` inside `namespace { ... }` (internal linkage: one private copy per translation unit) but referenced it from `inline void imgui_test_ensure_context(...)`/`imgui_test_begin_frame`/`imgui_test_end_frame` (vague linkage: the linker keeps exactly ONE translation unit's compiled body for the whole binary and discards the rest). With only `log_panel.test.cpp` including the header, one TU existed and nothing was wrong. The moment `console_panel.test.cpp` included the same header, two TUs each got their own private `g_imgui_test_ctx`, and the linker kept only one TU's copies of the three functions — so calls compiled from either `.cpp` file ran through whichever TU's bodies survived, silently manipulating a context pointer some *other* call site believed it owned. `debug` (`-O0`) happened to keep the state consistent enough to pass; `dev` (`-O1`+) did not, aborting inside `ImGui_ImplNull_NewFrame` on `GImGui != nullptr` — traced with a GDB backtrace after `fprintf` breadcrumbs inside the header showed the context correctly current one line before a crash reporting it as null one function call later, which only makes sense if the call landed in a different TU's copy of the code than the one that had just run. Fixed by making the variable a real `inline` VARIABLE (C++17) at namespace scope instead of an anonymous-namespace one, so it has external linkage and the linker merges every TU's reference to the single shared instance the `inline` functions were already assuming existed. The general rule: a variable an `inline` header function reads or writes must ITSELF be `inline` (or otherwise have external linkage) the moment more than one `.cpp` file might include that header — anonymous-namespace state is safe only behind functions that are also anonymous-namespace/`static`, never behind `inline` ones, and this class of bug is invisible until a second consumer exists and can differ by optimization tier alone, so it will not show up from a single test file or a debug-tier-only run.
- **`g_static = BigType{}` value-initializes a TEMPORARY the size of the whole type before assigning it — and an unoptimized build actually puts that temporary on the stack.** `foundation/prof.cpp`'s `tl_prof_test_reset()` wrote `g_prof = ProfState{}` to clear a ~53 MB namespace-scope static (`ProfFrame ring[60]`, `sizeof(ProfFrame) == 788520`); on the `dev`/`netcode` tiers (`-O1`+) the compiler elided the temporary into an in-place zero, so every local test run and even the CI `dev`/`netcode` legs passed. The `debug` tier (`-O0`) does not elide it: `ProfState{}` becomes a real 53 MB stack object, an instant segfault, on every one of CI's four `debug` legs (both OSes, both ISAs) with zero output from the crashing test (a stack overflow segfaults before any of the test's own assertions run, so `--isolate`'s per-test result showed FAIL with no failure text at all — the tell that it wasn't a logic bug). `git status`-clean, "verified locally," genuinely was: nobody had run the `debug` tier at all, only `dev` and `netcode`. Fixed with `memset(&g_prof, 0, sizeof(g_prof))`, which zeroes the existing static in place with no temporary of any size. The general rule: never write `big_static = BigType{}` (or `= BigType()`, or return a big aggregate by value into one) for a type whose size approaches or exceeds a stack frame — construct in place or `memset`, and validate a `-O0` tier locally at least once for any change to a large static, since `-O1`'s optimizer can make the bug invisible everywhere else. **Second confirmed instance, same session, different tier gate this time — a size threshold matters, not just optimization level.** `foundation/log.cpp`'s `tl_log_test_reset()` had the identical shape, `g_log = LogRing{}` (`LogRecord` is 248 B, `slot[4096]` makes the temporary ~0.97 MB) — an order of magnitude smaller than `prof.cpp`'s ~53 MB, so it fit inside Linux's 8 MB default stack on every tier including `debug` (`-O0`) and passed everywhere this lane validated. It only crashed on CI's `build-test (windows-*, debug)` legs, because Windows' default thread stack is 1 MB, not 8 MB, and the crash needed one more thing to tip it over: `editor/console`'s `Editor` struct had just grown by a ~53 KB `ConsoleState`, so a test constructing `Editor ed;` on the stack and then calling `tl_log_test_reset()` pushed the combined frame past 1 MB. Same zero-output, fastest-of-its-siblings signature as the `prof.cpp` case — a stack overflow crashes before any assertion runs, so it presents as a test that recorded no checks at all. The corollary: **this bug class is a function of the SMALLER of (stack budget, temporary size)** — a ~1 MB temporary is "safe" on an 8 MB Linux stack and a live grenade on a 1 MB Windows one, so "it passed on debug already" is not clearance; grep for the pattern (`grep -rn '= \w\+{};' src/foundation/*.cpp`, or generally any `g_x = T{}`/`g_x = T()` on a namespace-scope static) across a codebase the first time this bug class is found, not just fix the one instance that happened to get caught. **Third confirmed instance, same session — the second instance's own "grepped, no third exists" claim was wrong; the actual pattern needs the whole tree, not one directory.** `foundation/probe.cpp`'s `tl_probe_test_reset()` had the identical shape, `g_probe = ProbeState{}` (`ProbeKey keys[1024]` at 96 B each plus a 64 KB `staging` buffer, ~160 KB) — found not by CI but by re-running the pattern grep against all of `src/` (not just `src/foundation/*.cpp`, the scope the second instance's fix commit actually used) while building the Probes panel, which needed to touch this exact file anyway for an unrelated API promotion. Never observed failing (160 KB alone doesn't clear even a 1 MB Windows stack on its own the way the first two combined-with-something-else did), so this is a preventive fix, not a diagnosed regression — but the fix is free (`memset`) and the alternative is waiting for a fourth CI-red before someone re-runs the grep with the right scope. **Restated precisely this time: grep the pattern against the WHOLE tree (`grep -rn '= [A-Za-z_][A-Za-z0-9_]*{};' src/`), not a directory that merely contained the last instance found — a scoped-down "no third exists" claim is not evidence, it's an unexamined assumption wearing the language of one.**
- **Sanitizer red cannot be reproduced in this container — the ASan runtime is absent — so diagnose from source, state the confidence split, and let CI adjudicate.** `cmake --preset sanitize-linux` configures and compiles, then dies at link: `ld: cannot find /usr/lib/llvm-18/lib/clang/18/lib/linux/libclang_rt.asan-x86_64.a`. `libclang-rt-18-dev` and `libclang-common-18-dev` are already-newest and do not supply it; the whole `clang/18/lib/linux/` directory is missing. Two sessions have now hit this — PR #14's round-3 reviewer recorded it as "cannot discriminate locally", and the steward hit it again on PR #16's `sanitizers` red. The method that worked both times: **read the diff for the invariant it broke rather than guessing at the sanitizer's output.** On #16 the failing legs were the only red ones and the diff had just stood up an ImGui context in tests; `tests/editor/imgui_test_util.h` turned out to call `ImGui::CreateContext()` with zero occurrences of `vendor_glue_imgui_install`/`pool_vendor_init`, against the invariant `src/vendor_glue/imgui_glue.h` states in its own contract block, with the correct install→create→destroy pattern already sitting in `tests/vendor_glue/imgui_glue.test.cpp`. That is findable by grep in a minute. **Split the claim when you report it:** *high* confidence that the invariant violation is a real defect (verified from source, correct pattern in-tree), *unverified* that it is the cause of those specific legs — and name CI as the falsifier. On #16 CI then confirmed it. The same split protects the lane from chasing a confident-sounding wrong lead, and it costs nothing when you turn out to be right. Corollary: a vendored library stood up in a test harness needs its pooled-allocator hook installed *before* first use and something to free it at exit, or ASan reports the leak and the "every vendor allocator pooled and gated" property is quietly false in that harness.
- **A commit whose CI run is cancelled by the next push ships UNVERIFIED, and the run history hides it as a gap rather than a failure.** The editor lane pushed `e51966a` (the Console panel) and, before its run finished, pushed the `main` merge `88edf34` on top; GitHub's concurrency group cancelled the first, so the run list reads 257 (`c21da73`, success) → **no 258** → 259 (`88edf34`, failure). Nothing is marked red or skipped — the commit simply has no row, and both the lane and the steward read the branch as "last CI was green, then I merged docs." It was not: `e51966a` had never been exercised by CI at all, and it carried the only code change in the whole window (the `main` side of that merge was provably docs-only — `git diff --name-only c21da73...a2e020f` returns just `TODO.md` and `LESSONS.md`), so the Windows-debug failure that surfaced on the merge belonged entirely to the un-run commit. Two consequences worth keeping. **For bisection:** the last *green sha* and the last *CI-verified sha* are different facts, and only the second bounds a regression — enumerate the run list by `head_sha` and look for the missing numbers before blaming a merge for what the commit under it did. **For lanes:** a lane that validates locally and pushes twice in quick succession has bought itself nothing; if the second push is a merge, let the first run finish, or accept that the merge's run is the first real verdict on both commits at once. Local validation does not substitute — `e51966a`'s own message records four tiers validated both isolated and non-isolated, all on Linux, which is exactly the axis the failure was not on.
- **A test helper that passes a pointer into an `init` function which STORES it (rather than copying the pointee) needs the pointee to outlive every later call through that stored pointer — and a helper that only ever calls the init half never notices it got this wrong.** Every panel test file this lane wrote shared one `make_editor(Editor* ed)` helper: `VMemApi api = test_vmem_api(); return editor_init(ed, &api, 0u) == ERR_OK;` — `api` a plain LOCAL, `&api` handed to `editor_init` → `vmem_arena_init(&ed->dev_arena, ..., os)`, which stores the raw pointer as `VMemArena::os` (`vmem_arena.cpp`'s own `a->os = os;`) rather than copying the `VMemApi` struct's contents. The moment `make_editor` returns, `api`'s stack slot is dead; `ed->dev_arena.os` is now dangling. Five earlier panels (Log, Console, Inspector, Profiler, Probes) never noticed, because none of them ever called `arena_push` on `ed->dev_arena` — a dangling `os` pointer is invisible until something actually dereferences `a->os->commit(...)`. The World panel's "rehash arenas" feature was the first caller of `arena_push` on `ed->dev_arena` in ANY panel test, and it segfaulted with a genuinely confusing backtrace: the faulting PC's numeric value matched `ed`'s own pointer bit-for-bit, because a LATER stack frame (the test body calling into `world_panel_rehash_arenas`) had reused the exact stack slot `api` used to occupy and happened to write `ed`'s address there, so the dangling `a->os->commit` function-pointer call jumped to "the bytes now sitting where `api.commit` used to be" — which were `ed`'s address, not a real function. This is the SAME root shape `dotpath.test.cpp`'s `dp_fixture()` and `inspector.test.cpp`'s `ins_fixture()` had already found and fixed for a different stored pointer (`Interner`'s `VMemApi`/`VMemArena`s, both explicitly commented `"api MUST be static too: VMemArena stores a pointer to it..."`) — but that fix was never generalized to the `make_editor()` helper duplicated in six other test files, because nobody connected "this is the same bug class" until the sixth file's feature happened to be the first to actually exercise the dangling path. Fixed by making every `make_editor()`'s `VMemApi api` a `static` local (`test_vmem_api()` is a pure, stateless value-returning function — `VMemApi`'s only "state" is a handful of free-function pointers plus a null `ctx`, so a single process-lifetime copy is exactly as correct as a fresh one every call, and now actually outlives every arena that stores a pointer to it). **The general rule, stated precisely this time so it generalizes instead of being fixed per-symptom: any `init(..., const T* p)`-shaped function whose contract STORES `p` rather than copying `*p` turns every later access through the initialized object into a use of `p`'s pointee — a helper is only safe to write with a local `T` if NOTHING it initializes is used after the helper returns AND after anything might reuse the stack. When in doubt, grep every `init`-shaped function's contract comment for "stores"/"pointer to" before deciding a local is safe, not just for THIS call site's own current callers.**
- **A narrow check reported as a class-wide claim is worse than no check — a lane reads "ruled out" as permission to stop looking.** Twice in one session the steward verified one candidate and briefed the exclusion at the width of the whole class. (1) Diagnosing a Windows-debug-only silent test failure it computed `sizeof(Editor)` (~55 KB against Windows' 1 MB default stack), correctly concluded that local was not the culprit, and told the lane "RULED OUT: stack overflow — do not spend a cycle on it." The failure WAS a stack overflow, from `tl_log_test_reset()`'s `g_log = LogRing{}` materialising a 1,015,808-byte temporary that the same commit's ~53 KB `ConsoleState` growth tipped past the limit; the lane recovered only because the same brief happened to carry the discriminator. (2) It then reported "no third instance of the pattern remains in `src/`" after a grep anchored to line start (`^\s*g_[a-z_]+ = ...`), while every one of these resets is written as a ONE-LINER function body putting the assignment mid-line — a pattern that structurally could not match, written moments after reading that exact shape. Two rules. Write an exclusion at the width you actually verified ("Editor's own size is not it; a stack overflow from some other frame is still open"). And when the claim is "no more of X exist", run the pattern over the KNOWN instances first and confirm it matches them — a negative grep is only as strong as its demonstrated recall.
- **Never gate a lane on a condition it cannot observe — that is R-15's failure mode in its commonest disguise.** The steward told the editor lane "do not start the next panel until PR #16 is green." The lane cannot see CI: it fixed the bug, pushed, posted its PR comment, and went IDLE inside a minute on a wait that could never end from its side. Structurally identical to an earlier "round 3 follows; I spawn it, you do not need to ask" stall — the second time the steward manufactured the very silence R-15 exists to prevent. A lane's stopping condition must be something the lane itself can evaluate: a local test run, a doc criterion, its own diff. Never a remote fact only the steward can see — CI colour, another PR's state, a review verdict. Where the work genuinely depends on a remote fact, invert it: let the lane keep working and make the STEWARD responsible for interrupting on the bad case, since the steward is the party who can observe it. `WORKFLOW.md` §1 R-6 already says a lane should not stop after pushing, so check any new instruction against R-6 before sending it.
- **A done criterion can be unreachable from the day it is written, and nobody notices until the lane is five-sixths through it.** `TOOLING.md` §9.6's "v0 editor done" bundled six headless-testable panels together with a shell depending on `PLATFORM.md` §9.7 step 5 — unbuilt, no lane queued, and `struct PlatformDevApi` defined nowhere in the tree. The lane shipped five panels against that criterion without ever checking the finish line was reachable; the steward found it only by reading the clauses one by one while chasing an unrelated question, and it had to be split into `panels v0` / `shell v0` (RR-40) with the lane nearly done. Nothing in the process inspects a done criterion until it is time to claim it, which is exactly too late. Check REACHABILITY at lane start: for each clause, name the code that will satisfy it and confirm that code's dependencies exist in-tree — a clause naming a type defined nowhere is one grep on day one. Corollary: a criterion in doc A that silently depends on an unbuilt step in doc B is invisible to both lanes, so either XREF carries that dependency or the criterion names its blocker inline.

- **The width of a claim includes the RANGE of the command that produced it — an extension of the narrow-check-reported-as-class-wide entry above, with a second mechanism.** That entry warns about verifying one CANDIDATE and briefing the exclusion at the width of the whole class. The same failure arrives a second way: verifying over one RANGE and stating the result at the width of the tree. Two instances in one steward session, both by the steward. (1) Resolving a base-merge conflict it checked `git diff --numstat` on the two conflicted files, got zero deletions against both parents, and reported "strict superset of both parents, zero deletions against either" — true of those two files, false tree-wide, where the merge deletes 134 lines (all ordinary lane rewrites of lines `main` never touched, so the resolution was sound and only the sentence overreached). The reviewer checked it and corrected the record. (2) Diagnosing a `commit_docs` failure it ran the gate with a local `HEAD` that predated the commits in question, saw a commit go unflagged, and briefly suspected a hole in the gate; the gate was right and the range was wrong. **Two rules. Put the scope INSIDE the claim — "zero deletions in the two conflict files", not "zero deletions". And when the question is about pushed state, run against `origin/<branch>`, never `HEAD`: a stale local ref silently answers a different question and returns a confident, wrong, green.** **Where the check actually goes, learned by tripping over this a THIRD time in the same session after writing the two rules above — a briefing said "one commit, docs only" of a range holding two commits, one of them code.** All three misses share a shape the wording above does not capture: the command was CORRECT every time, and the claim about it was wider. So a habit attached to running the command cannot fire — there is nothing wrong at that moment. The trigger belongs on **writing the sentence**: before a claim leaves the turn, name the command that produced it and check the claim's scope is not wider than the command's. It is a check on the sentence, not on the measurement, because the sentence is where all three failed. (Formulation from the reviewer that caught the third instance; a reviewer watching one's errors across rounds sees the shape that the person making them does not.)

- **A green gate is not evidence that the other gates ran.** CI's `audits` job went red on a lane whose report was otherwise a model of evidence: four build tiers, isolated and non-isolated, real failure output watched before every fix, `includes.py` and `docaudit.py` per commit. It never ran `commit_docs.py` — the gate that failed — and the steward read that report, judged the evidence excellent, and did not notice the absent gate either. `docaudit` and `commit_docs` are different tools checking different things; a green one says nothing about the other. **Derive the local pre-push checklist FROM `.github/workflows/pr.yml`'s audit steps rather than carrying it from habit, and when reviewing a validation report, check it against the gate SET CI actually runs — not against the outputs it chose to show you.** A validation list is itself a claim, and an omission in it is invisible precisely because everything present is green.

- **A fact with a short half-life must be re-checked at the moment of use, not the moment of report.** A steward verified `mergeable_state: clean` at 18:41 and reported it; `main` advanced at 18:42:40; it launched a review at 18:46 against a PR that had been conflicted for four minutes. The check was correct when run and false when acted on, and nothing in between was wrong. This is the same shape as the doc-rot class `docaudit` structurally cannot see — a statement true when written that the world falsifies underneath it — but for remote state rather than prose. **Re-verify volatile remote facts (`mergeable_state`, branch heads, CI conclusions, another session's status) immediately before the action that depends on them. Once per turn is not once per action.**

- **A gate you own and have deliberately left red masks the next failure.** Once one CI job is red pending a fix the steward has taken ownership of, the overall red/green signal stops carrying information, and a second, genuine failure landing in the same run looks identical to the one already known. **When a known failure will persist for a while, switch from watching the rollup to watching per-check results, and name the known-red explicitly in the watch — so that "still only `audits`" and "something new broke" remain distinguishable.**

- **A queued message reaches an autonomous lane only at its next turn boundary, and R-6 tells lanes not to stop at commit boundaries — so a correction can be several commits late by construction.** A steward sent a lane a CI-red correction at 19:50; the lane, mid-turn on a long work list, committed at 19:55, 19:55 and 19:58 repeating the same fault, having never seen it. Nothing malfunctioned: R-6 and the message channel are working exactly as specified, and they compose into a delivery latency of one whole lane turn. Interrupting to force delivery is worse — an interrupt drops the queue, including the message being forced. **A correction that must land before the lane's next commit cannot be delivered by a queued message. It is either the steward's own to execute (the `WORKFLOW.md` R-13 shape), or it belongs in the lane's brief up front as a rule the lane can evaluate for itself. Send the message anyway for the record, then plan as though it will arrive late.**

- **A brief's effort estimate is a claim, and an inherited one is usually wrong.** A review area was briefed to a fresh reviewer as "small in lines, ~300"; it was ~1,395 added lines, with one file at +1,121. The estimate had been carried from an earlier plan rather than derived. An under-estimate is not a harmless nicety: it licenses precisely the confident skim the rest of the brief warns against. **Derive every area's size from `git diff --stat` at the moment the brief is written, and state the real number in the brief.**

- **A disjoint review split finds seam defects that no single reviewer can, and the finding appears BETWEEN the coverage lists rather than inside any one of them.** One criterion — "an fx edit of 1.5 into a `pos_t` field yields raw `0x60000`" — was examined by three reviewers owning code, tests and docs. The foundation reviewer proved the quantizer correct by mutation testing. The docs reviewer proved every substantive assertion survived the amendment that had been made to the criterion. The panels reviewer proved the test never reaches the walker at all: mutating the `FieldKind`→FRAC table so a typed `1.5` writes `0x18000` left all 74 editor tests green. Each was right within its own area; each alone would have reported that row satisfied. **Where a criterion spans code, tests and prose, split ownership across all three, make the partition exhaustive and disjoint, and require each reviewer to publish what it checked CLEAN as well as what it found — the gap lives in the seam, and only the coverage lists make the seam visible.**

- **File the craft lesson at the moment you notice it, not at closeout.** A steward held its accumulated lessons in a scratch file to avoid merge churn on an open PR; that was ruled wrong the same morning, on the grounds that durable context lives in committed files only. Its successor then deferred its own filings for the identical reason — an open PR heavily editing `LESSONS.md`, a lane actively pushing to the branch — and had to be told again. The reasoning feels prudent each time and is wrong each time: a session can end on any turn, an unfiled lesson dies with it, and the merge cost being avoided is a single append-region conflict that is always resolvable by keeping both sides. **Append to `LESSONS.md` on `main` when the lesson is fresh and pay the append conflict later. The conflict is cheap and mechanical; the lost lesson is neither.**

- **A merge performed through the GitHub merge API stamps the API TOKEN's identity on the merge commit, not the repository's author identity — and GitHub signs it, so the result is the exact thing this repo's public-repo protocol forbids: a bot identity carrying a green Verified badge.** PR #16 merged correctly in every respect a reviewer had been asked to check — real merge commit, two parents, never squashed, branch auto-deleted, green CI, three `ship` verdicts, and all 72 underlying commits carrying Rafael's author and committer identity — and then the merge commit itself came out `author: claude[bot] <…@users.noreply.github.com>`, `committer: GitHub`, `verified: true`. The three preceding lane merges were clean because they were done with a local `git merge` and a push; nothing anywhere said so, and the steward inherited "merge commit, never squash or rebase" and verified the METHOD scrupulously while never once checking the IDENTITY the method would produce. Fixed by rebuilding the merge locally from the same two parents, confirming the resulting tree was bit-identical to the API merge's, and force-pushing `main` with a lease — Rafael's call, not the steward's, because rewriting a public default branch is not an agent's decision to take alone. **Merge a PR with `git merge --no-ff` locally and push; do not use the merge API or the merge button.** The wider rule, which is the part worth carrying: **a rule stated as a property of an artifact ("commits carry Rafael's identity") is not self-enforcing at the moment a TOOL creates that artifact on your behalf.** Every checklist item here was about the shape of the merge and every one of them passed; the identity was assumed to follow from doing the merge, and it followed from HOW the merge was performed instead. When a tool creates a commit for you, check the commit it actually created — the same way a test is only evidence once you have watched it fail.
