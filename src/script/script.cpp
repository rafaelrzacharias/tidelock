// Placeholder TU for tl_script - a static lib needs one source. Replaced by the lane that owns
// this module (docs/ROADMAP.md §2).
#include "foundation/tl_types.h"

extern const u32 tl_script_unit;
const u32 tl_script_unit = 0;
