// Placeholder TU for tl_core - a static lib needs one source. Replaced by the lane that owns
// this module (docs/ROADMAP.md §2).
#include "foundation/tl_types.h"

extern const u32 tl_core_unit;
const u32 tl_core_unit = 0;
