// Placeholder TU for tl_net - a static lib needs one source. Replaced by the lane that owns
// this module (docs/ROADMAP.md §2).
#include "foundation/tl_types.h"

extern const u32 tl_net_unit;
const u32 tl_net_unit = 0;
