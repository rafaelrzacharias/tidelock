// Placeholder TU for tl_render - a static lib needs one source. Replaced by the lane that owns
// this module (docs/ROADMAP.md §2).
#include "foundation/tl_types.h"

extern const u32 tl_render_unit;
const u32 tl_render_unit = 0;
