// Placeholder TU for tl_foundation_det - a static lib needs one source. The W1 fx / mem /
// containers / rng lanes replace it with real sources (docs/ROADMAP.md §2); delete it then.
#include "foundation/tl_types.h"

extern const u32 tl_foundation_det_unit;
const u32 tl_foundation_det_unit = 0;
