// Placeholder TU for the headless impl (real file/clock/vmem/entropy/threads; null window/draw).
// Owned by the W1 platform lane (docs/PLATFORM.md §9).
#include "foundation/tl_types.h"

extern const u32 tl_platform_headless_unit;
const u32 tl_platform_headless_unit = 0;
