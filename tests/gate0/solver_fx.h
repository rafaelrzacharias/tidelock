#pragma once
// solver_fx.h - the fixed-point binding of the solver (namespace g0). Include this, never
// g0_ops.h/solver.h directly; the double binding is solver_dbl.h (dev tiers only).
#undef GATE0_SHADOW
#include "gate0/g0_ops.h"
#include "gate0/solver.h"
